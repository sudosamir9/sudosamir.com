# Exam trap inventory (consolidated)

Every trap defined anywhere in this study guide, in one place. 292 traps with stable IDs, collected from the ten blueprint topic files, the foundations refresher, and the four reference files that define traps of their own: precedence, time modifiers, regular expressions, and the CIM catalogue. Nothing here is invented: every ID traces to a `## Traps` block in a real file, and the "Where it is taught" column names the file and the heading where the underlying material is explained rather than the trap block itself, because the trap block is the summary and the heading is the argument.

Read this file twice. The first pass is the master table, working top to bottom, covering the whole surface. The second pass is the top twenty plus the final-review list, in the last hour.

## How the columns work

**ID** is stable across the whole guide. If a cram sheet or a self-check answer says "this is T-05-07", it means the row below.

**Blueprint section** is the exam section the trap bites in, not necessarily the file it lives in. Traps that come from a reference file rather than a blueprint topic file (`T-00-nn` foundations, `T-KO-nn` precedence, `T-TM-nn` time, `T-RX-nn` regular expressions, `T-CIM-nn` CIM catalogue) carry a parenthesised section showing where the exam is most likely to test them.

**The wrong belief** is written as a candidate would actually be thinking it, in the present tense, without hedging. That is the point. You do not recognise a trap by its correct statement; you recognise it by the shape of the thought you were about to have.

**Where it is taught** gives the filename and the heading. All topic files are under `topics/`. `knowledge-object-precedence.md`, `time-modifiers.md`, `regex-for-splunk.md` and `cim-data-models.md` are under `reference/`.

## Master table

| ID | Section | The wrong belief | The correct fact | Where it is taught |
|---|---|---|---|---|
| T-00-01 | 0.0 (2.0) | Fast is the default search mode. | Smart is the default. Fast and Verbose are the two ends; Smart sits in the middle and is what every new search and every saved report starts in. | 00-foundations-refresher.md, ## 1. Search modes |
| T-00-02 | 0.0 (1.0) | Smart mode always discovers all fields. | Smart discovers all fields only for non-transforming searches. Add a transforming command and Smart turns field discovery off and behaves like Fast. | 00-foundations-refresher.md, ## 1. Search modes |
| T-00-03 | 0.0 (9.0) | Verbose is just Smart with more fields, so it is always safe. | A report running in Verbose mode cannot use report acceleration. Switching an accelerated report to Verbose silently reverts it to full-scan speed. | 00-foundations-refresher.md, ## 1. Search modes |
| T-00-04 | 0.0 (4.0) | Interesting Fields means fields present in 20 percent of the events in the index or the source type. | The threshold is 20 percent of the events returned by this search. Change the search and the list changes. | 00-foundations-refresher.md, ## 2. The fields sidebar |
| T-00-05 | 0.0 | The Time Range Picker overrides `earliest` and `latest` in the search bar because clicking is more explicit. | Backwards. Time specified in the search bar or in a saved search overrides the picker. | 00-foundations-refresher.md, ### Inline modifiers |
| T-00-06 | 0.0 | Snapping with `@` rounds to the nearest boundary. | Snapping always rounds down, to the latest time not after the specified time. `-30m@h` at 14:50 gives 14:00, never 15:00. | 00-foundations-refresher.md, ### Relative time syntax |
| T-00-07 | 0.0 | In `-2h@h` the snap happens first, then the offset. | The offset left of `@` applies before the snap. A further offset written after the snap unit, as in `@d-2h`, applies after it. The two forms mean different things. | 00-foundations-refresher.md, ### Relative time syntax |
| T-00-08 | 0.0 (3.0) | A subsearch runs last, or runs as a stage of the outer pipeline. | The subsearch runs first and its results are substituted into the outer search before the outer search executes. | 00-foundations-refresher.md, ## 5. Subsearches |
| T-00-09 | 0.0 (3.0) | Subsearch limits are 50,000 results and 30 seconds, and `inputlookup` is exempt. | The documented defaults are 10,000 results and 60 seconds, set by `maxout` and `maxtime` in the `[subsearch]` stanza of limits.conf. Exceeding either finalizes silently, with no error. | 00-foundations-refresher.md, ## 5. Subsearches |
| T-00-10 | 0.0 (3.0) | `format` joins rows with AND and fields within a row with OR. | Backwards. The default is AND within a row, OR between rows, the whole set parenthesised, output into a single field named `search`. | 00-foundations-refresher.md, ### The format command |
| T-00-11 | 0.0 (1.0) | `table` is a formatting command, so it is not transforming. | `table` appears in the official transforming commands list beside `stats`, `chart`, `timechart`, `top` and `rare`. Command types are not mutually exclusive: `append` is transforming and dataset processing, `history` is transforming and generating. | 00-foundations-refresher.md, ## 4. Command types |
| T-00-12 | 0.0 (1.0) | `sort` is streaming because it seems to handle events one at a time. | `sort` is a dataset processing command: it needs the whole dataset before it emits anything. So are `eventstats`, `transaction`, `dedup`, `join` and `union`. | 00-foundations-refresher.md, ## 4. Command types |
| T-00-13 | 0.0 | `scanCount` and `eventCount` are the same number. | `scanCount` is events read off disk; `eventCount` is the subset that matched. In a sparse search `scanCount` is far larger. | 00-foundations-refresher.md, ## 9. The Job Inspector |
| T-00-14 | 0.0 (5.0) | App sharing makes a knowledge object visible in every app. | App sharing exposes it to users of the one app it lives in. Visibility everywhere requires All apps, which the docs call global. | 00-foundations-refresher.md, ## 10. Knowledge object management |
| T-00-15 | 0.0 | Reassign Knowledge Objects can fix any orphaned object. | It cannot reassign an object that is both orphaned and privately shared. Those need the owner account recreated or a manual configuration file edit. | 00-foundations-refresher.md, ## 10. Knowledge object management |
| T-00-16 | 0.0 (9.0) | Report acceleration, data model acceleration and summary indexing are three names for one feature. | Three mechanisms, three storage models. Report acceleration needs a transforming search with only streamable commands before the transform and at least 100,000 events in the covered hot bucket. Data model acceleration accelerates a model. Summary indexing is a scheduled search you build that writes into an index you nominate. | 00-foundations-refresher.md, ## 7. Reports and alerts (OFF BLUEPRINT) |
| T-00-17 | 0.0 (5.0) | Calculated fields can call a lookup, since both happen at search time. | Calculated fields are step 6, lookups are step 7. A calculated field cannot reference a lookup, an event type or a tag. A lookup can reference a calculated field. | 00-foundations-refresher.md, ## 10. Knowledge object management |
| T-00-18 | 0.0 | `_time` is stored in the indexer's local time zone. | `_time` is stored as UNIX epoch seconds, an absolute value, rendered in the viewing user's time zone. Two users in different zones see the same events with different displayed timestamps. | 00-foundations-refresher.md, ## 3. Time |
| T-00-19 | 0 (off blueprint) | A job only exists when a user types SPL into the search bar, so dashboards and reports have nothing to inspect. Wrong. Running a search, creating a pivot, opening a report and loading a dashboard panel each create a job, which is why a six-panel dashboard creates six of them and why every one of them shows up on the ho. | . | 00-foundations-refresher.md, ## Traps |
| T-00-20 | 0 (off blueprint) | The Search Job Inspector only works on a search that is still running, so you have to catch a slow search in the act. Wrong. Any job whose artifact has not expired can be inspected, running or not, succeeded or failed. What defeats you is expiry: an ad hoc job lives 10 minutes by default, and sharing it extends the lif. | . | 00-foundations-refresher.md, ## Traps |
| T-01-01 | 1.0 | `limit=10` caps the results table at ten rows. | On `chart` the default is `top 10` and it limits the number of columns, and `limit` is only valid when a column-split is specified. On `timechart` the default is `top10` distinct split-by values; row count is governed by `span` or `bins`. | 01-transforming-commands.md, ### chart |
| T-01-02 | 1.0 | `timechart count BY host, status` produces a nested chart. | `timechart` allows only one BY clause. `_time` is already the row-split, so the single BY field is the column-split. `chart count BY host status` is legal because `chart` has two slots. | 01-transforming-commands.md, ### timechart |
| T-01-03 | 1.0 | `chart count OVER host BY status` and `chart count OVER status BY host` give the same table. | The field after `OVER` is the row-split and becomes the first column and the X-axis; the field after `BY` is the column-split. Swapping them transposes the table. `chart count BY host status` equals `chart count OVER host BY status`. | 01-transforming-commands.md, ### chart |
| T-01-04 | 1.0 | `useother=f` removes the `NULL` column. | `usenull` controls the series for events missing the split-by field, labelled by `nullstr` (default `NULL`). `useother` controls the series for values excluded by `limit` or the where-clause, labelled by `otherstr` (default `OTHER`). Both default to true on `chart` and `timechart`. | 01-transforming-commands.md, ### chart |
| T-01-05 | 1.0 | `bins` defaults to 100 everywhere. | `chart` bin options default to `bins=300`; `timechart` bin options default to `bins=100`. Both set a maximum, not a target, so you can get far fewer bins. | 01-transforming-commands.md, ### timechart |
| T-01-06 | 1.0 | Specifying both `bins` and `span` narrows the result twice, or errors. | If you specify both, `span` is used and `bins` is ignored. | 01-transforming-commands.md, ### timechart |
| T-01-07 | 1.0 | `top 5 host` shows an `OTHER` row for the remaining hosts. | `useother` defaults to `false` on `top`, so no `OTHER` row appears unless you ask. On `chart` and `timechart` it defaults to `true`. | 01-transforming-commands.md, ### top and rare |
| T-01-08 | 1.0 | `rare` operates identically to `top`, so every `top` option works on it. | The 10.4 `rare` page lists only `countfield`, `limit`, `percentfield`, `showcount` and `showperc`. It never documents `useother` or `otherstr`, despite its own summary line claiming the options match `<top-options>`. Answer from the option list. | 01-transforming-commands.md, ### top and rare |
| T-01-09 | 1.0 | `chart cont=false partial=false` is valid. | `partial` (default true) and `fixedrange` (default true) exist only on `timechart`. `cont` exists on both and defaults to true on both. | 01-transforming-commands.md, ### timechart |
| T-01-10 | 1.0 | `stats ... BY a b` and `chart ... BY a b` return the same table with different formatting. | With `stats`, each row is one unique combination and both group-by fields stay as columns. With `chart`, the first BY field becomes rows and the second becomes columns, so the second field's values become column headers and its name disappears. | 01-transforming-commands.md, ## Result contract |
| T-01-11 | 1.0 | `chart sum(A) BY A span=log2` bins field A and sums it. | With `chart` and `timechart` you cannot use the same field in a function and as the row-split or split-by field. The documented workaround is `eval A1=A` then `chart sum(A) BY A1 span=log2`. | 01-transforming-commands.md, ### chart |
| T-01-12 | 1.0 | `timechart count BY categoryId span=1d` sets a daily time bucket. | Specify `bins` and `span` before the split-by field. Placed after it, Splunk assumes you are binning the split-by field, not the time axis. Write `timechart span=1d count BY categoryId`. | 01-transforming-commands.md, ### timechart |
| T-01-13 | 1.0 | `span=1d@d` or `span=1mon@mon` work in `timechart`. | You can only use week spans with the snap-to span argument in `timechart`, for example `span=w@w1`. Separately, `aligntime` is ignored when `span` is in days, months or years. | 01-transforming-commands.md, ### timechart |
| T-01-14 | 1.0 | `table` is transforming, so `table host status` is enough to render any visualization. | `table` is listed as transforming and is exactly right for a scatter chart because it fixes column order to marker, X, Y. But it aggregates nothing: a pie chart needs an aggregation producing two columns, and the docs warn that `eval` or `fields` can change result structure so that column and bar charts cannot render. | 01-transforming-commands.md, ## Result contract |
| T-01-15 | 1.0 | `timechart sparkline(count) BY host` adds inline mini-charts. | `sparkline` applies only to the `chart` and `stats` commands. Its size is capped by `sparkline_maxsize` in limits.conf. | 01-transforming-commands.md, ### chart |
| T-01-16 | 1.0 | `per_hour(price)` forces hourly buckets. | `per_day()`, `per_hour()`, `per_minute()` and `per_second()` are aggregator functions usable with `timechart` and are not responsible for setting a time span. At a 30 minute span, `per_hour()` returns `sum()*2`. Hourly buckets still need `span=1h`. | 01-transforming-commands.md, ### timechart |
| T-01-17 | 1.0 | `limit=0` suppresses the series entirely. | On `chart`, `limit=0` returns all results. On `timechart` it means all distinct values, no series filtering. On `top` and `rare` it returns all values up to `maxresultrows`, default maximum 50,000. | 01-transforming-commands.md, ### top and rare |
| T-01-18 | 1.0 | `xyseries` is transforming, so it can end a search that feeds a visualization on its own. | `xyseries` is distributable streaming unless `grouped=true`, and `grouped` defaults to false. `untable` is always distributable streaming. Neither turns an event list into a statistics table; they reshape results a transforming command already produced. | 01-transforming-commands.md, ### The shape converters |
| T-01-19 | 1.0 | You can combine `limit=5` with `WHERE count > 100` to get at most five series above a threshold. | The `limit` and `agg` options are ignored if an explicit where-clause is provided. The default with no where-clause is equivalent to `where sum in top10`. | 01-transforming-commands.md, ### chart |
| T-01-20 | 1.0 | `rare 5 user` is the mirror image of `top 5 user`. | Documented syntax is `top [<N>] [<top-options>...] <field-list>` but `rare [<rare-options>...] <field-list>`. Only `top` documents the leading `<N>`, and `top limit=<int>` is the same as `top N`. Write `rare limit=5 user`. | 01-transforming-commands.md, ### top and rare |
| T-01-21 | 1.0 | `chart count BY vendor_action, user` works because `chart` supports two BY clauses, so `timechart count BY host, status` should work for the same reason. | the documented syntax is one BY clause taking a row-split and a column-split, `BY <row-split> <column-split>`, comma optional. `timechart` has one BY clause too; it just accepts a single field in it, because `_time` already holds the row-split. Count fields, not clauses. | 01-transforming-commands.md, ## Traps |
| T-01-22 | 1.0 | `chart sales BY product_name` reports the `sales` field split by product. | the docs state you must specify a statistical function when you use the `chart` command, so the argument must be a stats-agg-term, a sparkline-agg-term, or a parenthesised eval expression. The working form is `chart sum(price) AS sales BY product_name`. Bare field names belong to `table` and `fields`, which aggregate n. | 01-transforming-commands.md, ## Traps |
| T-01-23 | 1.0 | `stats sum(price) AS sales OVER product_name` is the `stats` way of naming an X-axis. | the `stats` syntax offers only `BY <field-list>`. `timechart` has no `OVER` because `_time` is its fixed row-split, `xyseries` takes three positional fields, and `transaction` takes a field list. `OVER` on any other command is wrong on syntax alone. | 01-transforming-commands.md, ## Traps |
| T-01-24 | 1.0 | an `interval` or `duration` argument sets the bin size. | the documented bin options are `span`, `bins`, `minspan`, `start`, `end`, and `aligntime`. `span` sets the bin size, `bins` caps the bin count, and no `interval` argument exists. `duration` is a field the `transaction` command outputs, which is what makes it plausible. | 01-transforming-commands.md, ## Traps |
| T-02-01 | 2.0 | `index=web clientip=ipaddress` finds events where the two fields hold the same value. | `search` expects a field compared to a literal and reads `ipaddress` as the string `ipaddress`. Use `where clientip=ipaddress`, and for the negative either `where fieldA!=fieldB` or `where NOT fieldA=fieldB`. | 02-filtering-and-formatting.md, ### search |
| T-02-02 | 2.0 | AND always binds tighter than OR. | `search` evaluates OR before AND, so `host="www1" AND status=200 OR action="addtocart"` runs as `host="www1" AND (status=200 OR action="addtocart")`. `eval` and `where` evaluate AND before OR, giving `(host="www1" AND status=200) OR action="addtocart"`. `search` also has no XOR; `eval` and `where` do. | 02-filtering-and-formatting.md, ### search |
| T-02-03 | 2.0 | `NOT status=404` and `status!=404` return the same events. | `status!=404` returns only events that have a `status` field with a different value, dropping events with no `status`. `NOT status=404` returns everything except the matches, including events with no `status`. Corollary: `NOT field=*` returns events where the field is null, and `field!=*` never returns anything. | 02-filtering-and-formatting.md, ### search |
| T-02-04 | 2.0 | SPL is either all case sensitive or all not. | For `search`, field names are case sensitive, field values are not, and searches are case-insensitive unless wrapped in `CASE()`. `eval` and `where` are case sensitive throughout, and `like()` matches case-sensitively. | 02-filtering-and-formatting.md, ### search |
| T-02-05 | 2.0 | `coalesce(X,Y)` behaves like `if()`, testing X and returning Y. | `coalesce(<values>)` takes one or more arguments and returns the first that is not NULL. No test expression, no branches. `if(<predicate>,<true_value>,<false_value>)` is the conditional and takes exactly three arguments. This is the Apress Table 2-16 error verbatim. | 02-filtering-and-formatting.md, ### Evaluation functions |
| T-02-06 | 2.0 | `null()` takes arguments and behaves like `nullif()`. | `null()` takes no arguments and returns NULL, clearing a field when assigned. `nullif(<field1>,<field2>)` takes two fields and returns NULL when they are equal, otherwise `<field1>`. The second Apress Table 2-16 error. | 02-filtering-and-formatting.md, ### Evaluation functions |
| T-02-07 | 2.0 | A value matching no `case` condition falls through to the last value listed. | `case` returns NULL when no condition is TRUE. The documented default idiom is a final `true(), "<default>"` pair. The mirror is `validate`, which returns the value for the first FALSE condition and defaults to NULL when all are TRUE. | 02-filtering-and-formatting.md, ### Evaluation functions |
| T-02-08 | 2.0 | `eval matched = isnull(user)` puts true or false into `matched`. | The result of an eval expression cannot be a Boolean, so this errors. Wrap it in `if()`, `case()` or `validate()`, or convert with `tostring()`. The same restriction hits `in()`, `like()`, `cidrmatch()`, `searchmatch()` and the whole `is*` family under `eval`. Under `where` they are used directly, because `where` wants a Boolean. | 02-filtering-and-formatting.md, ### eval |
| T-02-09 | 2.0 | `where useragent="Mozilla*"` filters on a prefix. | `where` has no asterisk wildcard; `*` is a literal there. Use `like(useragent, "Mozilla%")` or `useragent LIKE "Mozilla%"`. | 02-filtering-and-formatting.md, ### where |
| T-02-10 | 2.0 | The `IN` operator and the `in()` function are the same thing and both take wildcards. | `IN` works with `search` and `tstats` and accepts `*`, as in `status IN (40*, 500)`. The `in()` function works with `eval`, `where` and `fieldformat`, requires quoted values, and accepts no wildcards; under `eval` it must be nested in `case`, `if` or `validate`. | 02-filtering-and-formatting.md, ### Evaluation functions |
| T-02-11 | 2.0 | The `fillnull` default value is NULL, an empty string, or N/A. | The default is `0`. Bare `fillnull` writes `0` into every null cell of every field. `value=` precedes the field list in the syntax. | 02-filtering-and-formatting.md, ### fillnull |
| T-02-12 | 2.0 | `fillnull value=0 bytes*` fills every field starting with `bytes`. | `fillnull` takes a plain `<field-list>` with no wildcard support, and naming a field that does not exist creates it, so `bytes*` creates a literal field called `bytes*`. `filldown` is the command whose `<wc-field-list>` accepts `*`. | 02-filtering-and-formatting.md, ### fillnull |
| T-02-13 | 2.0 | `fillnull` and `filldown` both write a constant. | `fillnull` writes one constant everywhere. `filldown` copies the last non-null value seen above that row and leaves the cell NULL when there is no previous value. | 02-filtering-and-formatting.md, ### filldown |
| T-02-14 | 2.0 | `eval test2=null()` then `fillnull value=NULL` produces a `test2` column of NULL strings. | A field exists in the schema only if it has at least one non-null value in the result set, so an all-null field is invisible to `fillnull` and drops out. The exception is naming it explicitly in the field list, which creates it. | 02-filtering-and-formatting.md, ### fillnull |
| T-02-15 | 2.0 | `fieldformat bytes=tostring(bytes,"commas")` then `sort - bytes` sorts on the formatted value, and a later `outputcsv` writes the commas. | `fieldformat` changes rendering only; later commands and export commands such as `outputcsv` and `outputlookup` see the original value. Use `eval` when the change must persist, and place `fieldformat` last. One expression per `fieldformat` command. | 02-filtering-and-formatting.md, ### fieldformat |
| T-02-16 | 2.0 | Multivalue and string indexing start at the same place. | `mvindex()` and `mvfind()` index from 0, with `-1` as the last value and NULL for out-of-range. `substr()` indexes from 1 with SQLite semantics, negative indexes counting from the end and the length argument optional. | 02-filtering-and-formatting.md, ### Evaluation functions |
| T-02-17 | 2.0 | "Create a field with an eval expression" always means the `eval` command. | The `eval` command creates a field for one search. A calculated field defined in props.conf or through Settings creates it for every search touching that source type, and its result is directly searchable in the base search, which an in-pipeline `eval` result is not. | 02-filtering-and-formatting.md, ### eval |
| T-02-18 | 2.0 | `stats count BY host` then `search error` finds rows whose events contained `error`. | `stats` and `chart` do not pass `_raw` down the pipeline, so a later `search` can only use field-value pairs on the surviving fields. Bare keywords have nothing to match. | 02-filtering-and-formatting.md, ### search |
| T-02-19 | 2.0 | `tostring(x,"commas")` preserves all decimals, and any format keyword is accepted. | Only `"binary"`, `"hex"`, `"commas"` and `"duration"` are supported. `"commas"` rounds to two decimal places; `"duration"` converts seconds to `HH:MM:SS`. A currency symbol is not a format option, so concatenate: `"$" . tostring(x,"commas")`. | 02-filtering-and-formatting.md, ### Evaluation functions |
| T-02-20 | 2.0 | `\| eval size = tostring(bytes,"commas") \| sort - size` still orders by size, because the number survives underneath. Correct: `tostring` returns a string and the assignment overwrites the field with it, so `sort` uses lexicographical collation and orders on the first character. Sort while the values are numeric and c. | . | 02-filtering-and-formatting.md, ## Traps |
| T-02-21 | 2.0 | the field an `eval` creates is written to the index, a KV Store collection, or a lookup, so the next search can filter on it. Correct: it lands in a field in the search results only and is gone when the search ends, so naming it in a new base search matches nothing. Only a calculated field, or an explicit `outputlookup. | . | 02-filtering-and-formatting.md, ## Traps |
| T-02-22 | 2.0 | `eval` turns into a transforming command once it creates a column or once it follows `stats`, or it is "report generating". Correct: `eval` is distributable streaming wherever it sits. Position changes only the machine, a distributable streaming command running on the indexers unless it follows a non-streaming command,. | . | 02-filtering-and-formatting.md, ## Traps |
| T-03-01 | 3.0 | `maxspan` and `maxpause` have positive defaults such as `30s`, `5m` or `1000`. | Both default to `-1`, meaning no limit, the constraint deactivated. Only `maxevents` has a positive default, `1000`. | 03-correlating-events.md, ## Syntax and options |
| T-03-02 | 3.0 | `transaction` sorts its own input, so `eval x=1` before it is harmless. | `transaction` requires incoming events in descending chronological order, and `sort` must occur immediately before it. Ascending order with `maxspan` returns incorrect results rather than an error. | 03-correlating-events.md, ## Syntax and options |
| T-03-03 | 3.0 | `duration` is in minutes, or is null for a one-event transaction. | `duration` is the difference in seconds between the timestamps of the first and last events, and a single-event transaction has `duration=0`. Convert with `eval` for minutes. | 03-correlating-events.md, ## Result contract |
| T-03-04 | 3.0 | The `_time` of a transaction is the latest event, or the time the transaction closed. | The transaction takes the time and date fields of the earliest member event. | 03-correlating-events.md, ## Result contract |
| T-03-05 | 3.0 | Any boundary option closes a transaction, so setting `endswith` should give `closed_txn=1`. | `closed_txn` is set to `1` only if one of `maxevents`, `maxspan`, `maxpause` or `startswith` is specified. `endswith` is not in that list. If none of the four is given, all transactions are output with `closed_txn=0`. | 03-correlating-events.md, ## Result contract |
| T-03-06 | 3.0 | `keeporphans=true` marks results with `orphan=1` or reuses `closed_txn=0`. | Orphan results carry a `_txn_orphan` field with value `1`. The option default is `false`. | 03-correlating-events.md, ## Result contract |
| T-03-07 | 3.0 | `mvlist=false` suppresses multivalue fields entirely, and `true` is the default. | The default is `false`, which renders multivalue fields as a set of unique values ordered alphabetically. `mvlist=true` renders them as a list of the original event values in arrival order. You can pass a field list to make only those fields render as lists. | 03-correlating-events.md, ## Syntax and options |
| T-03-08 | 3.0 | `unifyends` has an independent default of `true`. | Its default is `true` because it is set to the same default value as `connected`. Setting `connected=false` makes `unifyends=false`. | 03-correlating-events.md, ## Syntax and options |
| T-03-09 | 3.0 | `stats` and `transaction` are equivalent, or `transaction` wins because it produces fewer results. | `stats` is a transforming command and performs more efficiently, especially in a distributed environment. `transaction` is a centralized streaming command that only runs on the search head. | 03-correlating-events.md, ## Decision rules |
| T-03-10 | 3.0 | `transaction clientip` then `where count>5` filters to transactions of more than five events. | `transaction` produces `eventcount`. `count` is what `stats` produces. That filter returns nothing. | 03-correlating-events.md, ## Result contract |
| T-03-11 | 3.0 | `append` and `transaction` compose freely, so `append [search ...]` then `transaction sessionid` works. | You cannot use `transaction` after `append`. Use `stats` to group after an append. | 03-correlating-events.md, ## Decision rules |
| T-03-12 | 3.0 | `first()` returns the chronologically earliest value, and `latest()` is its synonym. | `first()` returns the first seen value in processing order, which is the most recent instance; `last()` returns the oldest instance in processing order. For time order use `earliest()` and `latest()`. Processing order is not necessarily chronological order. | 03-correlating-events.md, ## Decision rules |
| T-03-13 | 3.0 | `values()` and `list()` differ only in name. | `values()` returns distinct values in lexicographical order with no default cap. `list()` returns every value in event order and returns only the first 100 if a field has more than 100 values. | 03-correlating-events.md, ## Decision rules |
| T-03-14 | 3.0 | `maxevents=-1` means no events are allowed. | A negative value deactivates the constraint, exactly as for `maxspan` and `maxpause`. | 03-correlating-events.md, ## Syntax and options |
| T-03-15 | 3.0 | `startswith` and `endswith` take regular expressions. | A `<filter-string>` is a search expression, a quoted search expression in parentheses, or `eval(<eval-expression>)` returning a Boolean. Use `eval(match(...))` if you genuinely need a regex. | 03-correlating-events.md, ## Syntax and options |
| T-03-16 | 3.0 | `transaction host cookie` means host AND cookie, a strict conjunction. | Splunk does not necessarily interpret multiple fields as a conjunction or a disjunction. If a transitive relationship exists and the events appear in the right sequence, `transaction` uses it. A result with no `host` value can join a transaction with `host=mylaptop`, while `host=mylaptop` and `host=myserver` can never share one. | 03-correlating-events.md, ## What it is |
| T-03-17 | 3.0 | `join` is the general-purpose correlation answer and scales like SQL. | `join` defaults to `max=1` subsearch result per main result, and the right-side dataset defaults to a maximum of 50,000 rows over a maximum runtime of 60 seconds. The docs recommend `stats` and `transaction` over `join` and `append`. | 03-correlating-events.md, ## Decision rules |
| T-03-18 | 3.0 | `connected=false` is a way to change time behaviour. | `connected` is only relevant when a field or field list is specified, and it governs one narrow case: an event that carries the required fields but where none of those fields has yet been instantiated in the open transaction. | 03-correlating-events.md, ## Syntax and options |
| T-03-19 | 3.0 | Transactions can never exceed 1000 events; that is a product ceiling. | `1000` is the `maxevents` default, settable per search or per `transactiontypes.conf` stanza, and a negative value removes it. The real ceilings are `maxopentxn` (5000 by default in limits.conf) and `maxopenevents` (100000 by default), and those evict rather than truncate. | 03-correlating-events.md, ## Syntax and options |
| T-03-20 | 3.0 | `keepevicted` and `keeporphans` do the same thing. | `keepevicted=true` outputs transactions left open by memory pressure or a missing close condition, identified by `closed_txn=0`. `keeporphans=true` outputs events that never belonged to any transaction, identified by `_txn_orphan=1`. Both default to `false`. | 03-correlating-events.md, ## Result contract |
| T-03-21 | 3.0 | `max_events_per_bucket` controls the per-transaction event ceiling. | `max_events_per_bucket` sits in the `[search]` stanza, where it caps the events retrieved for each timeline bucket in searches with `status_buckets>0`, and has nothing to do with `transaction`. The `[transactions]` stanza holds only `maxopentxn` and `maxopenevents`, both also settable as memory control options on the c. | 03-correlating-events.md, ## Traps |
| T-03-22 | 3.0 | the options that shape a group come back as fields. | `maxspan`, `maxpause`, and `maxevents` are inputs only. The command adds `duration` and `eventcount`, sets `closed_txn`, and creates `_txn_orphan` and `transactiontype` only under the relevant options. No field named `transaction` exists either, so filters shaped like `where transaction="REJECT"` match nothing. | 03-correlating-events.md, ## Traps |
| T-03-23 | 3.0 | pre-filtering on a term is the same as filtering the finished groups. | the pre-pipe term discards every event that does not carry it, so each session contributes only its `REJECT` events and you get one-event transactions with `duration=0`. Group first, then `\| search REJECT`, which matches the preserved `_raw` of any member and returns the whole session. | 03-correlating-events.md, ## Traps |
| T-03-24 | 3.0 | `streamstats` is a cheaper `stats`. | `streamstats` adds running aggregates to each event as it passes and returns every event; it never collapses events into a group. The grouping commands are `stats` and `transaction`, recommended by the docs over `join` and `append`. | 03-correlating-events.md, ## Traps |
| T-04-01 | 4.0 | After saving an extraction you must reindex, or the field only appears on data ingested from now on. | The Field Extractor creates a search-time extraction only. It applies retroactively to every already-indexed event of that source type or source, and needs no reindex and no restart. | 04-field-extractions.md, ### The Field Extractor workflow |
| T-04-02 | 4.0 | A field created by `rex` shows up in the fields sidebar for future searches and can be managed under Settings. | `rex` is a search command, not a knowledge object. Its fields exist for the duration of that one pipeline and appear nowhere in Settings. | 04-field-extractions.md, ### rex |
| T-04-03 | 4.0 | Every FX extraction ends with Select Fields, Validate, Save. | Validate exists only on the regular expression path. The delimiter path is Select Sample, Select Method, Rename Fields, Save. Counterexamples are a Validate-step feature and do not exist for delimiter extractions. | 04-field-extractions.md, ### The Field Extractor workflow |
| T-04-04 | 4.0 | The default permission at the Save step is App, so the extraction is immediately visible to everyone in the app. | The default is Owner, and the docs say it "only extracts fields in searches run by the person who created the extraction." The three choices are Owner, App, All apps. | 04-field-extractions.md, ### The Field Extractor workflow |
| T-04-05 | 4.0 | Field values are case sensitive. | Field names are case sensitive, field values are not. `sourcetype=x` and `SOURCETYPE=x` differ, but `status=Failed` matches `failed`. The exception the exam builds on: `eval` and `where` expressions are case sensitive, so `where action="Purchase"` will not match `purchase`. | 04-field-extractions.md, ## Result contract |
| T-04-06 | 4.0 | `eventtype` and `tag` are default fields extracted at index time. | The documented default field list is `host`, `index`, `linecount`, `punct`, `source`, `sourcetype`, `splunk_server`, `timestamp`, plus internal `_raw`, `_time`, `_indextime`, `_cd`, `_bkt`, plus the `date_*` fields. Event types and tags are applied at search time, stages 8 and 9. | 04-field-extractions.md, ## Result contract |
| T-04-07 | 4.0 | Interesting Fields are the fields Splunk considers important, and the 20 percent threshold controls what gets extracted. | Interesting Fields lists fields appearing in at least 20 percent of the returned events. It is a display rule for the sidebar only. A field in 5 percent of events is fully extracted and fully searchable, just filed under All Fields. | 04-field-extractions.md, ## Result contract |
| T-04-08 | 4.0 | `rex` requires a `field=` argument. | `field` defaults to `_raw`. `erex` uses `fromfield`, which also defaults to `_raw`. `extract` has neither and can only read `_raw`. | 04-field-extractions.md, ### rex |
| T-04-09 | 4.0 | `max_match=0` means no matches, or `max_match` is unlimited by default. | The default is `1`, and `0` means unlimited. Any value above 1 produces a multivalue field. If your pattern has a capture group that can match repeatedly within itself, only the last capture group is used for the multiple matches. | 04-field-extractions.md, ### rex |
| T-04-10 | 4.0 | `erex` is the command-line Field Extractor and persists what it builds. | `erex` extracts for that search only and prints its generated regular expression to the job messages, readable from the Job menu. It is a regex-writing aid. It is also absent from the Apress book entirely. | 04-field-extractions.md, ### erex |
| T-04-11 | 4.0 | You can always back out of a manual regex edit in the FX. | You can select Back to abandon manual editing "only if you have not yet tried to preview a regular expression change." Once you preview an edited regex, the interactive highlighting workflow for that extraction is gone. | 04-field-extractions.md, ### The Field Extractor workflow |
| T-04-12 | 4.0 | `extract` takes a `field=` argument like `rex`. | It does not. `extract` works only on `_raw`. The documented workaround is `rename _raw AS temp uri_query AS _raw`, then `extract`, then `rename _raw AS uri_query temp AS _raw`. | 04-field-extractions.md, ### extract (alias kv) |
| T-04-13 | 4.0 | In the delimiter method you must rename every discovered field. | You must select and rename at least one field to reach Save, and you do not have to rename them all. Unrenamed fields keep their auto-assigned names `field1`, `field2`, `field3` and so on. | 04-field-extractions.md, ### The Field Extractor workflow |
| T-04-14 | 4.0 | The FX always writes the same configuration whichever method you choose. | The regular expression method writes an inline `EXTRACT-<class>` into props.conf with the regex embedded. The delimiter method writes a transforms.conf stanza holding `DELIMS` and `FIELDS` plus a `REPORT-<class>` reference in props.conf. This is why changing permissions on a delimiter extraction needs a manual transforms.conf move. | 04-field-extractions.md, ### What the FX writes |
| T-04-15 | 4.0 | Delimiters are mostly used for unstructured data. | Choose Delimiters when values are cleanly separated by a common character and are consistent across events, "commonly the case with structured, table-based data such as .csv files or events indexed from a database." Choose Regular Expression for unstructured data such as a system log. This is the Apress answer-key error verbatim. | 04-field-extractions.md, ## Decision rules |
| T-04-16 | 4.0 | The Event Actions entry point starts the wizard at Select Sample. | Opening the FX from Event Actions, Extract Fields on a specific event starts you at Select Method, because the source type and sample event are already determined. Every other entry point starts at Select Sample. | 04-field-extractions.md, ### The two entry points the exam cares about |
| T-04-17 | 4.0 | A saved delimiter extraction can later be reopened and edited as a regular expression. | The two methods produce different configuration objects. A saved delimiter extraction is a transform built on `DELIMS` and `FIELDS` with no `REGEX` setting, so there is no regular expression to open, and Validate does not apply. Within the wizard, before saving, you can back up and change the method. [verify] | 04-field-extractions.md, ### What the FX writes |
| T-04-18 | 4.0 | Sample size does not matter. | The field extractor is limited to twenty lines on a sample event, so a very long multiline event cannot be fully sampled. Separately, automatic search-time key-value extraction finds only the first 100 obvious `key=value` pairs per event, and `extract` defaults to `limit=50` pairs and `maxchars=10240` characters. | 04-field-extractions.md, ### extract (alias kv) |
| T-04-19 | 4.0 | . | the docs describe the delimiters method as designed for structured event data, "data from files with headers, where all of the fields in the events are separated by a common delimiter, such as a comma or space". The header row lives in the file, not inside each event, which is why the Rename Fields step gives you `fiel. | 04-field-extractions.md, ## Traps |
| T-04-20 | 4.0 | events without the required string stop appearing in searches. | required text "behaves like a search filter" for the extraction only. Events that lack the string are still returned by the search, they just do not get the extracted fields. Two limits go with it: one required-text string per field extraction, and required text may not overlap with text already highlighted as a field . | 04-field-extractions.md, ## Traps |
| T-04-21 | 4.0 | . | on a fresh search Selected Fields contains exactly `host`, `source`, and `sourcetype`. `index` is a default field on every event, so it clears the 20 percent bar and appears under Interesting Fields, not Selected Fields. All Fields is the dialog you use to promote anything else into the selected list. | 04-field-extractions.md, ## Traps |
| T-04-22 | 4.0 | Any delimiter character can be chosen from the delimiter list. | the Rename Fields step offers four presets, Space, Comma, Tab, and Pipe. Every other delimiter, a colon or a semicolon for instance, has to be typed into the **Other** box. Such delimiters work perfectly well; they are simply not on the button list. | 04-field-extractions.md, ## Traps |
| T-04-23 | 4.0 | New Field Extraction on the Field extractions page opens the Field Extractor. | it opens a manual configuration form where you type the stanza, the type, and the regex or transform name yourself. The wizard is behind the **Open field extractor** button on the same page. | 04-field-extractions.md, ## Traps |
| T-05-01 | 5.0 | After aliasing `clientip` to `src_ip`, searching `clientip=*` returns nothing. | An alias does not replace a field in an event or remove it from an event; it is added alongside. Both names work. The command that actually replaces a name is `rename`, and it only affects the current result set. | 05-aliases-and-calculated-fields.md, ### Field alias, Splunk Web |
| T-05-02 | 5.0 | `rename clientip AS src_ip` is the same thing as a field alias. | `rename` is a search command operating on the result set downstream of all nine search-time operations, dropping the old name, lasting one search. An alias is a knowledge object that runs at step 5, keeps the old name, applies to every search against the scoped host, source or source type, and is subject to app scope and permissions. | 05-aliases-and-calculated-fields.md, ## Result contract |
| T-05-03 | 5.0 | `FIELDALIAS-x = userID AS user loginID AS user` merges both source fields into `user`. | A field can have multiple aliases, but a single alias can only apply to one field. That configuration is documented as invalid and only one of the two takes effect. The documented answer is `EVAL-user = coalesce(userID, loginID)`. | 05-aliases-and-calculated-fields.md, ### 2. One field, two aliases |
| T-05-04 | 5.0 | The Overwrite field values checkbox is on by default, and it controls whether the original field survives. | Overwrite field values is not selected by default, and it never touches the original field. It controls the destination field only: selected replaces an existing destination and deletes it when the original is missing; cleared leaves an existing destination alone and keeps it when the original is missing. | 05-aliases-and-calculated-fields.md, ### Field alias, Splunk Web |
| T-05-05 | 5.0 | Since lookups add fields to events, a calculated field's eval expression can use them. | Calculated fields are step 6 and lookups are step 7. Calculated fields can reference all types of field extractions; they cannot reference lookups, event types or tags. The docs promise an error message for this violation. | 05-aliases-and-calculated-fields.md, ### 6. What a lookup can and cannot see |
| T-05-06 | 5.0 | Define `EVAL-duration_ms`, then alias it to `duration` to give it a CIM name. | Aliasing is step 5 and calculated fields are step 6. The props.conf spec states you cannot alias a calculated field. Give the calculated field the CIM name in the first place, or use a second calculated field. | 05-aliases-and-calculated-fields.md, ## Result contract |
| T-05-07 | 5.0 | `EVAL-response_time = response_time/1000` followed by `EVAL-bitrate = bytes*1000/response_time` uses the converted seconds value. | All `EVAL-<fieldname>` configurations within a single props.conf stanza are processed in parallel instead of sequentially, so `bitrate` still divides by the original millisecond value. This produces a plausible wrong number, not a missing field. | 05-aliases-and-calculated-fields.md, ### 4. The parallel-evaluation trap, made visible |
| T-05-08 | 5.0 | The Eval expression box takes a full eval statement such as `eval kb = round(bytes/1024, 2)`. | The field name is a separate form field (Name in the UI, `<fieldname>` in `EVAL-<fieldname>`), and the Eval expression box holds the right-hand side only. No leading `eval` keyword, no assignment. | 05-aliases-and-calculated-fields.md, ### Calculated field, Splunk Web |
| T-05-09 | 5.0 | If `status` is already extracted, `EVAL-status` only fills in the gaps. | The calculated field overrides the extracted field, even if the eval statement evaluates to null. Preserving the extraction requires `EVAL-field = coalesce(field, <expression>)`, or `coalesce(<expression>, field)` if the extraction should only win when the expression is null. | 05-aliases-and-calculated-fields.md, ### 5. The coalesce pattern, both directions |
| T-05-10 | 5.0 | You must write `sourcetype=x \| search src_ip=1.2.3.4` because the alias is not real until the pipeline runs. | Both objects are applied during search-time schema construction, before the first pipe, so both are valid base-search terms. The docs demonstrate `source=eqs7day-M1.csv Description=Deep` against a calculated field. | 05-aliases-and-calculated-fields.md, ## Result contract |
| T-05-11 | 5.0 | The scope choices are host, source and event, or host, source and index. | Both the alias form and the calculated field form scope to a host, a source, or a source type. Nothing else. This is the same triple every props.conf stanza type maps to. The Apress answer key for question E encodes "Host, Source, Events". | 05-aliases-and-calculated-fields.md, ### Field alias, Splunk Web |
| T-05-12 | 5.0 | Alias a source type value, then point the `EVAL-` stanza at the alias. | Creation of a calculated field on an aliased source is not supported, and you cannot scope a calculated field to an aliased host, source or source type. The workaround is to filter inside the expression, for example `if(response_code=200,len(app),null)`. | 05-aliases-and-calculated-fields.md, ### Calculated field, props.conf |
| T-05-13 | 5.0 | The conf-file settings are `ALIAS-` and `CALC-`. | `FIELDALIAS-<class>` and `EVAL-<fieldname>`, both in props.conf, both with no default. The `EVAL` portion is not case-sensitive but the hyphen is required and the `<fieldname>` portion is case sensitive. Field aliases live in props.conf, not fields.conf, transforms.conf or aliases.conf. | 05-aliases-and-calculated-fields.md, ### Field alias, props.conf |
| T-05-14 | 5.0 | A newly created alias or calculated field is immediately visible to the whole team. | The knowledge object is private to you when created; other users cannot see or use it. Sharing to an app or globally is a separate step under Settings, Fields, then Permissions. By default only admin and power roles can share and promote, and a power user can only change permissions for objects they own. | 05-aliases-and-calculated-fields.md, ## Result contract |
| T-05-15 | 5.0 | Alias `eventStartTime` to `_time` to re-timestamp events at search time. | The docs warn against creating a field alias for a field with the same name as an internal field such as `_time`, because it produces unpredictable search results. | 05-aliases-and-calculated-fields.md, ### Field alias, props.conf |
| T-05-16 | 5.0 | a field present in two source types needs one alias, or the number needed depends on the index or on the original field names matching. | each field alias configuration is specific to events belonging to a particular host, source, or source type, so two source types need two alias definitions. Index is not a scope, and the pattern matching syntax is documented for the `host::` and `source::` stanza forms, not for a bare source type stanza. | 05-aliases-and-calculated-fields.md, ## Traps |
| T-05-17 | 5.0 | . | the eval statement is as flexible as it is for the `eval` search command, so it may read one field, several fields, a field alias, or no field at all. The real rule is directional: calculated fields can reference all types of field extractions and field aliases, and cannot reference lookups, event types, or tags. | 05-aliases-and-calculated-fields.md, ## Traps |
| T-05-18 | 5.0 | since `src_ip=DEEP` and `src_ip=deep` match the same events, `SRC_IP=deep` must work too. | an alias name is a field name, and field names are case sensitive while field values are not. | 05-aliases-and-calculated-fields.md, ## Traps |
| T-06-01 | 6.0 | Tags attach to events, or to fields. | A tag attaches to a field-value pair, `<field>=<value>`. You tag `host=web01`, `sourcetype=secure`, `status=404` or `eventtype=failed_login`. The event picks up the tag only because it contains that pair. The commonest distractor pair is "applied to a field" versus "applied to a field-value pair". Choose the pair. | 06-tags-and-event-types.md, ## What it is |
| T-06-02 | 6.0 | The field-scoped tag search is `tag:<field>=<name>`, or `tag.<field>`, or `tag[field]`. | The form is `tag::<field>=<tagname>`, exactly two colons, no spaces. `tag=<tagname>` with no field scope searches every tagged field-value pair. Both forms accept `*`. | 06-tags-and-event-types.md, ### Tag search syntax |
| T-06-03 | 6.0 | An event type can contain a pipe as long as the pipe comes last. | The docs forbid a search that "includes a pipe operator after a simple search" and one that "includes a subsearch". Position does not rescue it. `sourcetype=access_combined_wcookie status=404 \| stats count` is an invalid event type definition regardless. | 06-tags-and-event-types.md, ### eventtypes.conf |
| T-06-04 | 6.0 | An event type can reference a report with `savedsearch`. | The same restriction list forbids referencing a report with the `savedsearch` command inside an event type definition. | 06-tags-and-event-types.md, ### eventtypes.conf |
| T-06-05 | 6.0 | Priority 10 is the highest priority. | Priority is an integer 1 through 10 and "1 is the best Priority and 10 is the worst". Lower number wins. The docs' example gives `critical_disk_error` priority 3 and `all_system_errors` priority 7, and 3 is the better value. | 06-tags-and-event-types.md, ### Splunk Web form fields |
| T-06-06 | 6.0 | Priority only affects sort order. | Priority does two things: event types with a priority are listed above those without and ordered by their priority value, and only one event type colour displays per event, so the colour of the best-priority match renders. Event types with no priority are ordered alphabetically below those that have one. | 06-tags-and-event-types.md, ### Splunk Web form fields |
| T-06-07 | 6.0 | `eventtype` is a single-valued field. | A single event can match multiple event types, and when it matches two or more, `eventtype` acts as a multivalue field. Splunk orders the values by priority score first, then lexicographically. | 06-tags-and-event-types.md, ## Result contract |
| T-06-08 | 6.0 | An event type is the same thing as a saved search or a report. | A report is a saved search you run, on demand or on a schedule, returning a result set. An event type is a classification applied automatically at search time to every matching event; you never run it, you search for the field value it produces. A report may contain pipes, transforming commands and subsearches. An event type may not. | 06-tags-and-event-types.md, ## Decision rules |
| T-06-09 | 6.0 | Tags run before event types, or lookups run after event types. | The order is field filters, inline extraction, transform-based extraction, automatic key-value extraction, field aliasing, calculated fields, lookups, event types, tags. Tags are last, event types second to last. So a tag can be applied to an event type; an event type can reference a lookup output field; a calculated field cannot reference an event type; a lookup cannot reference an event type or a tag; an event type cannot reference a tag. | 06-tags-and-event-types.md, ## Result contract |
| T-06-10 | 6.0 | Settings, Tags has one list. | It has three views: List by field-value pair, List by tag name, and All unique tag objects. Disabling a tag from the List by tag name page disables it across all apps that contain it. The All unique tag objects view exposes the individual tag-name to field-value-pair to app combination for per-object permission edits. | 06-tags-and-event-types.md, ### tags.conf |
| T-06-11 | 6.0 | You define tags for an event type using the `tags` attribute in eventtypes.conf. | That attribute exists but the spec marks it deprecated and points to tags.conf.spec. Tags on an event type live in tags.conf under a stanza header of the form `[eventtype=<name>]`, exactly like any other field-value pair. This is the CIM pattern. | 06-tags-and-event-types.md, ### tags.conf |
| T-06-12 | 6.0 | Tag names are case sensitive and field names are not. | Backwards. Field names are case sensitive, field values are not. A tag name is matched as a value of the `tag` pseudo-field, so `tag=WebServer` and `tag=webserver` match the same pairs, while `tag::Host=webserver` and `tag::host=webserver` are different searches. | 06-tags-and-event-types.md, ## Result contract |
| T-06-13 | 6.0 | Because tags cannot have priority or colour, a statement pairing tags with priority and colour must be entirely false. | You can set priority and colour on the event type, and you can then tag that event type. Priority and colour are never properties of a tag. The Apress question C is keyed false while the same chapter's worked example does exactly this. | 06-tags-and-event-types.md, ## Decision rules |
| T-06-14 | 6.0 | Tagging `host=web01` renames the host. | Tagging never changes the underlying field value. It adds a searchable alternate label. This is why tagging is the standard remedy when a host value changed for an input and historical events still carry the old value: tag both values with one tag and search the tag. | 06-tags-and-event-types.md, ## What it is |
| T-06-15 | 6.0 | The search term is `tags=<tagname>`, or a bare `tag::<fieldname>` finds everything tagged on that field. **Wrong on both counts.** The term is singular, `tag`, and the scoped form always ends with `=<tagname>`, because the tag name is what you match. Wrong options here are mutations of the two real forms: pluralised, s. | . | 06-tags-and-event-types.md, ## Traps |
| T-06-16 | 6.0 | Event types are defined in `props.conf`, or created with a `searchtypes` command. **Wrong.** Definitions are stanzas in `eventtypes.conf`, the stanza header being the event type name; `props.conf` holds extractions, aliases, calculated fields and lookup bindings, and has no `event_type` stanza. The discovery command is. | . | 06-tags-and-event-types.md, ## Traps |
| T-06-17 | 6.0 | An event type stores a time range, output formatting, a rank or a weight. **Wrong.** The stanza has five live attributes: `disabled`, `search`, `description`, `priority` and `color`. A stored time range and stored formatting belong to a report. Rank and weight are not event type concepts at all, so when they appear as . | . | 06-tags-and-event-types.md, ## Traps |
| T-07-01 | 7.0 | A macro is invoked as `'mymacro'` with quotation marks. | The delimiter is the backtick, the character sharing a key with the tilde. Single or double quotation marks do not invoke a macro; the text is just a search term. Any option using `'` or `"` around the macro name is wrong. | 07-macros.md, ## Syntax and options |
| T-07-02 | 7.0 | `mymacro(2)` means "the second form" or "use argument 2". | Verbatim: "if your search macro mymacro includes two arguments, name it mymacro(2)". You store `mymacro(2)` and invoke `` `mymacro(foo,bar)` ``. The stored name carries a count; the invocation carries values. | 07-macros.md, ## Syntax and options |
| T-07-03 | 7.0 | You write `$val$, $rate$` in the Arguments field to match the definition. | Arguments is "a comma-delimited string of argument names", written bare as `val, rate`, while the Definition references them as `$val$` and `$rate$`. | 07-macros.md, ## Syntax and options |
| T-07-04 | 7.0 | One quoting rule covers both macro references and argument tokens. | Two opposing rules. "The Splunk platform does not expand macros when they are inside quoted values", and separately "The Splunk platform replaces the $<arg>$ pattern globally in the string, even inside quotation marks." | 07-macros.md, ## Syntax and options |
| T-07-05 | 7.0 | Returning a string from a validation expression means success, and errormsg is always what the user sees. | A Boolean validation expression succeeds on true and fails on false or NULL, with errormsg supplying the message. A non-Boolean validation expression succeeds on NULL and fails when a string is returned, and the returned string is itself the message, so errormsg is unused. | 07-macros.md, ### 5. Non-Boolean validation with validate() |
| T-07-06 | 7.0 | A bad macro argument yields zero events. | Validation runs before dispatch, so the search does not run at all and the user sees the validation error message. | 07-macros.md, ### 4. Two arguments with Boolean validation |
| T-07-07 | 7.0 | Editing macros.conf requires a full restart. | The Admin Manual page on when to restart lists macros.conf among search-time files whose settings take effect without one; refreshing the endpoints is sufficient. The line in macros.conf.spec reading "You must restart the Splunk instance to enable configuration changes" is generic boilerplate copied into every spec file. The Apress guide states the restart claim outright. | 07-macros.md, ## Result contract |
| T-07-08 | 7.0 | Macro expansion sits among field aliases, calculated fields and lookups and obeys the same precedence reasoning. | The documented nine-step sequence contains no macro step, because expansion is textual and precedes parsing. | 07-macros.md, ## Result contract |
| T-07-09 | 7.0 | A macro definition starting with `tstats` or `inputlookup` keeps its leading pipe. | Remove the leading pipe from the definition and put it in the search in front of the reference, as `` \| `mygeneratingmacro` ``. | 07-macros.md, ## Decision rules |
| T-07-10 | 7.0 | The naming rule is the same for macro names and argument names. | Argument names "can only contain alphanumeric characters, underscores ( _ ), and hyphens ( - )" with no repeats, while the manual warns "Don't include macros with hyphens in your searches; the Search app doesn't support hyphens in macro names." | 07-macros.md, ## Syntax and options |
| T-07-11 | 7.0 | `[foobar]` and `[foobar(1)]` are one macro with an optional argument. | From the spec, "[foobar], [foobar(1)], [foobar(2)], and so forth" are not the same macro. Calling `` `foobar(x)` `` when only `[foobar]` exists is an error, not a call with an ignored argument. | 07-macros.md, ## Syntax and options |
| T-07-12 | 7.0 | Macro arguments are always positional. | From macros.conf.example, a two-argument macro "could be invoked equivalently as `foobar(1,2)` `foobar(foo=1,bar=2)` or `foobar(bar=2,foo=1)`". The documentation's own example uses the named form, `` `iis_search(fragment=TM)` ``. | 07-macros.md, ## Syntax and options |
| T-07-13 | 7.0 | `iseval` makes the macro run an eval command and put a computed value in your results. | `iseval = true` means "the 'definition' setting is expected to be an eval expression that returns a string representing the expansion of this macro", default false. `` `fooeval(10,20)` `` is replaced by the text `10 + 20`, which the surrounding search then has to make sense of. | 07-macros.md, ### 6. Eval-based definition |
| T-07-14 | 7.0 | A macro created in Search and Reporting is available everywhere. | A new knowledge object is associated with the app context it was created in, and app-level sharing makes it available only to users of that app. Sharing must be All apps to use it elsewhere, and only admin and power roles can share and promote by default. | 07-macros.md, ## Result contract |
| T-07-15 | 7.0 | arguments are defined when the macro runs, or the values are baked into the search string when the macro is saved. Correct: the Arguments field fixes the names at creation time, and the invocation supplies the values, which resolve the search string at execution by substituting into the `$name$` tokens. Validation is c. | . | 07-macros.md, ## Traps |
| T-07-16 | 7.0 | a macro with arguments must also carry a validation expression and an error message. Correct: the documented procedure marks every step except Name and Definition as "(Optional)", Destination app included. | . | 07-macros.md, ## Traps |
| T-07-17 | 7.0 | macros are configured in Settings, then Fields, with the other search-time objects, or under Searches, reports, and alerts because a macro is search text. Correct, verbatim: "Select Settings > Advanced Search > Search macros.". | . | 07-macros.md, ## Traps |
| T-07-18 | 7.0 | a macro has to end the search, or piping after a reference requires the macro to be shared globally. Correct: a reference behaves like the text it becomes, and the manual's own example pipes into one (`` sourcetype=access_* \| `mymacro` ``) with nothing stopping further commands after it. Sharing decides whether the ma. | . | 07-macros.md, ## Traps |
| T-08-01 | 8.0 | The Action type field offers GET, POST and Search. | Action type has exactly two values, `link` and `search`. GET and POST are both `link` actions, distinguished by Link method (`link.method = get` or `post`). In workflow_actions.conf, `type` accepts only `link` or `search`; there is no `type = get` and no `type = post`. | 08-workflow-actions.md, ### The Splunk Web form, in order |
| T-08-02 | 8.0 | With Open link in left untouched, the link opens in the current window, because that is how ordinary hyperlinks behave. | `link.target` defaults to `blank`, a new window. The same applies to `search.target`, which the spec says works in the same way as `link.target`. | 08-workflow-actions.md, ### workflow_actions.conf |
| T-08-03 | 8.0 | A stanza with `type = link` and a `link.uri` but no `link.method` does nothing, or is invalid. | `link.method` defaults to `get`, so a GET is issued. Only `type` and `label` are required for the object to be considered at all, plus `link.uri` for a link action and `search.search_string` for a search action. | 08-workflow-actions.md, ### workflow_actions.conf |
| T-08-04 | 8.0 | With "Apply only to the following fields" left blank, the action never appears, or appears only on events with no fields. | `fields` defaults to `*`, which matches all fields, so an unrestricted workflow action appears everywhere. Restriction is opt-in, not opt-out. | 08-workflow-actions.md, ### workflow_actions.conf |
| T-08-05 | 8.0 | `fields = src_ip, dest_ip` means the action shows when either field is present. | It is a conjunction. Every field listed must be present on the event before the action is applied. The documented example is explicit: `*, ip_client, ip_server` applies to all fields in the event if `ip_client` and `ip_server` are both present. | 08-workflow-actions.md, ### workflow_actions.conf |
| T-08-06 | 8.0 | POST arguments are appended to the URI, so a POST is just a GET with extra steps. | `link.postargs.<int>.key` and `.value` pairs are sent HTTP-form encoded in the request body, not in the query string, so they do not appear in the URL, browser history, or the target's access log query string. `link.postargs` is only available when `link.method = post`; setting post arguments on a GET action does nothing. | 08-workflow-actions.md, ### 3. POST, sending an event body to an issue tracker |
| T-08-07 | 8.0 | Tokens are escaped consistently everywhere, or nowhere. | Encoding differs by destination. Values inserted into `link.uri` are URI encoded automatically. POST argument values are HTTP-form encoded automatically. Values inserted into `search.search_string` are not escaped: the spec states it does not attempt to determine if inserted values may break quoting or search language escaping. Quote your own tokens in SPL. `$!fieldname$` suppresses escaping in a URI. | 08-workflow-actions.md, ### Token syntax |
| T-08-08 | 8.0 | With `search.earliest = -7d` and `search.preserve_timerange = true`, the originating search's range wins because the checkbox is on. | `search.preserve_timerange` is ignored if either `search.earliest` or `search.latest` is set. The range is the last seven days. To inherit the originating range, set `preserve_timerange = true` and leave both bounds empty. | 08-workflow-actions.md, ### 5. Search that inherits the originating time range |
| T-08-09 | 8.0 | `$@field_value$` resolves to the first field of the event, or to `_raw`, wherever the action appears. | `$@field_name$` and `$@field_value$` are not available for event menus. They only resolve when the click came from a field menu, so a generic per-field action must be scoped `display_location = field_menu`. | 08-workflow-actions.md, ### Token syntax |
| T-08-10 | 8.0 | The Name is what searchers see in the menu. | Name is the internal name and becomes the stanza header in workflow_actions.conf. Label is the text drawn in the menu, and Label is the field that accepts `$field$` tokens, which is what lets an entry read `Whois: 87.194.216.51` rather than `Whois: $clientip$`. The frequently repeated claim that workflow action names may not contain spaces is not supported by the documentation: the shipped spec examples include `[Create JIRA issue]` and `[Controller req over time]` [verify]. | 08-workflow-actions.md, ### The Splunk Web form, in order |
| T-08-11 | 8.0 | A workflow action is how you add a field to your results, or how you trigger an action automatically when a condition is met. | A workflow action creates no field, modifies no event, and fires only on a human click in Splunk Web. Automatic responses to conditions are alert actions on a saved search; enrichment at search time is lookups, calculated fields, field aliases and extractions. Only the workflow action is user-initiated from a menu. | 08-workflow-actions.md, ## What it is |
| T-08-12 | 8.0 | "Show action in" defaults to the field menu, since most examples are field-level. | `display_location` defaults to `both`, so an unconfigured action appears in the event menu and the field menus. | 08-workflow-actions.md, ### workflow_actions.conf |
| T-08-13 | 8.0 | Workflow actions are global by default because they are configured under Settings. | A newly created knowledge object is only available to the user who created it until explicitly shared, and by default only admin and power roles can share and promote. The Destination app chosen at creation sets the namespace that sharing then operates within. | 08-workflow-actions.md, ## Result contract |
| T-08-14 | 8.0 | With `search.app` and `search.view` omitted, the secondary search runs in Search and Reporting and the default search view. | `search.app` defaults to the current app and `search.view` defaults to the current view, meaning wherever the analyst happened to be when they clicked. | 08-workflow-actions.md, ### workflow_actions.conf |
| T-08-15 | 8.0 | workflow action names must not contain spaces or special characters. | no Splunk documentation states any such restriction. The `workflow_actions.conf.example` file shipped with the product defines the stanzas `[Create JIRA issue]` and `[Controller req over time]`, both containing spaces. The rule that does exist is the Name and Label split of T-08-10. | 08-workflow-actions.md, ## Traps |
| T-08-16 | 8.0 | a label is invalid unless it contains a field name surrounded by dollar signs. | a label can be static or include the value of relevant fields, so tokens are supported and never required. "Must include a token" is false; "can include a token" is true. | 08-workflow-actions.md, ## Traps |
| T-08-17 | 8.0 | configuring a workflow action includes choosing a sourcetype. | there is no sourcetype setting in `workflow_actions.conf` and none on the form. Scope is `fields` and `eventtypes` only. Listing `sourcetype` under `fields` does not help either, since that requires the field to exist and it exists on every event. Build an event type and name it in `eventtypes`. | 08-workflow-actions.md, ## Traps |
| T-08-18 | 8.0 | POST workflow actions are always encrypted. | nothing about the POST method encrypts anything; transport security depends entirely on whether `link.uri` is `https`. POST guarantees placement, not confidentiality. The arguments avoid the URL bar, browser history and the target's access log query string, and travel in plaintext over `http` just the same. | 08-workflow-actions.md, ## Traps |
| T-08-19 | 8.0 | the search string is capped at a fixed character count, or cannot use macros, or must run in the app the action lives in. | none of those is documented, and the app claim is refuted by `search.app` existing. The one stated limitation is on `search.search_string`, which does not attempt to determine whether inserted field values may break quoting or other search language escaping. The dispatched job is an ordinary search job started from the. | 08-workflow-actions.md, ## Traps |
| T-09-01 | 9.0 | Pivot provides a dataset for the data model. | The reverse. The data model provides the dataset, and Pivot is the SPL-free interface that consumes it to build a report. Pivot cannot run without a data model. The Apress answer key B states the wrong direction outright. | 09-data-models-and-pivot.md, ## What it is |
| T-09-02 | 9.0 | A child dataset does not inherit from its parent. | A child dataset inherits all constraints and all fields from every ancestor, then adds its own. Inheritance is cumulative and each level down returns strictly fewer events. The Apress answer key E marks this false. | 09-data-models-and-pivot.md, ## What it is |
| T-09-03 | 9.0 | The CIM data models ship with acceleration enabled. | "All data models included in the CIM add-on have data model acceleration turned off by default." You enable it per model on the CIM Setup page by selecting the model and checking Accelerate, which writes `acceleration = 1` for that model. The Apress answer key F states the opposite. | 09-data-models-and-pivot.md, ### Acceleration settings |
| T-09-04 | 9.0 | Acceleration is a property of the whole data model and applies to every dataset in it. | Acceleration requires at least one root event hierarchy, or one root search hierarchy that uses only streaming commands. Root search datasets built on transforming searches cannot be accelerated, and root transaction datasets and their children do not benefit. | 09-data-models-and-pivot.md, ### Acceleration settings |
| T-09-05 | 9.0 | You add auto-extracted fields wherever you need them. | Only root datasets can add auto-extracted fields; child datasets inherit them. Eval expression, lookup, regular expression and Geo IP fields can be added to any dataset. | 09-data-models-and-pivot.md, ### Field (attribute) types |
| T-09-06 | 9.0 | Required is a documentation hint or a Pivot form validation. | Required filters out any event that does not have the field, acting as another constraint on top of the formal constraints. Marking a sparse field Required shrinks your dataset without warning. | 09-data-models-and-pivot.md, ### Field attributes (metadata) |
| T-09-07 | 9.0 | A hidden field is removed from the dataset or from search results. | A hidden field is only suppressed for Pivot users defining a report. The field still exists in the dataset and is still reachable from SPL. | 09-data-models-and-pivot.md, ### Field attributes (metadata) |
| T-09-08 | 9.0 | A root event dataset constraint can contain a pipe and a command such as `stats count`. | Constraints "look like the first part of a search, before pipe characters and additional search commands are added". If you need a pipe you need a root search dataset, and you give up acceleration if that search is transforming. | 09-data-models-and-pivot.md, ### Dataset definition settings |
| T-09-09 | 9.0 | Both the Dataset Name and the Dataset ID can be renamed later. | Dataset Name accepts any character except asterisks and can be changed. Dataset ID accepts only alphanumerics, underscores and hyphens, cannot contain spaces, and cannot be edited after the dataset is saved. | 09-data-models-and-pivot.md, ### Dataset definition settings |
| T-09-10 | 9.0 | `tstats` and `from` use the same separator between model and dataset. | `tstats` uses `FROM datamodel=<model>.<root_dataset>` with an equals sign. `from` uses `from datamodel:<model>.<dataset>` with a colon. Options that swap the two are the standard distractor. | 09-data-models-and-pivot.md, ### SPL commands that read a data model |
| T-09-11 | 9.0 | `tstats` reads only summaries, or `summariesonly=true` is the default. | `summariesonly` defaults to `false`, and by default `tstats` runs over accelerated and unaccelerated data models alike. Setting it true restricts results to summarized data, which is faster and may be incomplete outside the Summary Range. | 09-data-models-and-pivot.md, ### SPL commands that read a data model |
| T-09-12 | 9.0 | `datamodel M D search` returns every field the dataset defines. | `strict_fields` defaults to `true`, returning only the default fields and the fields used in the dataset constraints. Set `strict_fields=false` to get all defined fields. | 09-data-models-and-pivot.md, ### SPL commands that read a data model |
| T-09-13 | 9.0 | Using Pivot on an unaccelerated model builds a summary you keep. | Ad hoc acceleration summaries live in the search head dispatch directory and are deleted when you leave the Pivot Editor or switch to a different dataset. Only persistent acceleration writes shared summaries to the indexers. | 09-data-models-and-pivot.md, ### Acceleration settings |
| T-09-14 | 9.0 | Any user can accelerate their own data model. | You cannot accelerate data models that are private; the model must be shared at app level or globally, and the user needs a role with the `accelerate_datamodel` capability. By default only admin and power roles can create data models at all. | 09-data-models-and-pivot.md, ### Acceleration settings |
| T-09-15 | 9.0 | A macro is just text substitution, so it is safe in any constraint. | Child dataset constraints cannot include search macros, and searches referencing child datasets whose constraints contain macros will fail. | 09-data-models-and-pivot.md, ### Dataset definition settings |
| T-09-16 | 9.0 | Geo IP output fields behave like any other calculated field and can be set Optional. | Geo IP fields are added as Required fields and their Type values are predetermined; you cannot change either. They also need an existing field of Type `ipv4` positioned above them. | 09-data-models-and-pivot.md, ### Field (attribute) types |
| T-09-17 | 9.0 | Field definition order is cosmetic. | Fields are processed in descending order from the top of the list to the bottom, so an eval expression that reads another calculated field must sit below it or it evaluates to null. | 09-data-models-and-pivot.md, ### Field attributes (metadata) |
| T-09-18 | 9.0 | You can open any search in Pivot. | The Pivot button on the Statistics or Visualization tab is available for non-transforming searches. When you save the resulting table or chart, the data model created behind it is private and can only be seen by its creator until an admin or power user shares it. | 09-data-models-and-pivot.md, ## Result contract |
| T-09-19 | 9.0 | An accelerated data model is frozen; you must build a new one to change it. | "After you accelerate a data model, you cannot edit it. To make changes to an accelerated data model, you must turn off its acceleration." It is uneditable while acceleration is on, but the remedy is to toggle acceleration off, edit, then re-enable, which discards and rebuilds the summaries. | 09-data-models-and-pivot.md, ## Traps |
| T-09-20 | 9.0 | Acceleration with several root datasets. The wrong belief is that when a data model has more than one root dataset, only the first one is accelerated. The correct fact is that every qualifying hierarchy in the model is summarized, and the docs state plainly that "Data models can contain a mixture of accelerated and una. | . | 09-data-models-and-pivot.md, ## Traps |
| T-09-21 | 9.0 | What acceleration does to fields. The wrong belief is that persistent acceleration turns all the fields in the model into indexed fields. The correct fact is that acceleration writes `.tsidx` summary files alongside the buckets on the indexers, holding the indexed field and value combinations for the accelerated datase. | . | 09-data-models-and-pivot.md, ## Traps |
| T-09-22 | 9.0 | Child datasets as a dataset type. The wrong belief is that a child of an event, search or transaction dataset is not one of the data model dataset types, because a data model has only three. The correct fact is that three is the count of root types. The docs count four dataset types overall, event, search, transaction . | . | 09-data-models-and-pivot.md, ## Traps |
| T-09-23 | 9.0 | `datamodel` argument order and mode names. The wrong belief is that the first name after `\| datamodel` is the dataset. The correct fact is model first, dataset second, mode third, because the dataset name must be specified after the data model name. Two distractors travel with this one: the acceleration mode is `accel. | . | 09-data-models-and-pivot.md, ## Traps |
| T-10-01 | 10.0 | The CIM normalizes data at index time, or changes how events are stored. | The CIM is a search-time schema, schema-on-the-fly, and it "leaves the raw machine data intact". Installing the add-on reindexes nothing and modifies no indexed event. | 10-cim.md, ## What it is |
| T-10-02 | 10.0 | CIM data models are accelerated out of the box, so installing the add-on immediately speeds up searches. | Acceleration is disabled by default for every CIM data model, and you enable it per model on the CIM Setup page. A `tstats summariesonly=t` search against a stock CIM model returns zero rows. This is the Apress answer key F error. | 10-cim.md, ### CIM Setup page |
| T-10-03 | 10.0 | Populating the CIM field names is sufficient for compliance. | Tags are the constraint that admits events into an event dataset. Correct field names without the required tag put the events nowhere. Compliance requires both the tags and the populated required fields. | 10-cim.md, ### Knowledge objects the add-on ships (objective 10.2) |
| T-10-04 | 10.0 | The default Indexes allowlist is empty, so the model searches no indexes until you configure one. | By default each data model searches all indexes. Leaving it that way is a performance problem, not a functional one. The old term "index whitelist" is the same setting. | 10-cim.md, ### CIM Setup page |
| T-10-05 | 10.0 | The CIM add-on is installed on indexers, forwarders, or all tiers. | Search heads only, because the add-on contains only search-time knowledge objects. | 10-cim.md, ## What it is |
| T-10-06 | 10.0 | "Compute Inventory" is a current CIM data model name. | The model is named Inventory in current CIM. "Compute Inventory" is the legacy documentation slug for that same page. The same trap works with Application State and Change Analysis, both deprecated in add-on version 4.12.0 and replaced by Endpoint and Change respectively. | 10-cim.md, ### The CIM 8.6 data model catalogue |
| T-10-07 | 10.0 | `datamodel Web Web search \| table action, status` is a working validation search. | `datamodel ... search` returns fields under their fully qualified dotted names, so the columns are `Web.action` and `Web.status`. The `flat` mode is what strips the prefix. The plain `table action` returns an empty column. | 10-cim.md, ### datamodel |
| T-10-08 | 10.0 | You can create a field alias for a lookup output field or for a tag. | Field aliases are applied after key-value field extraction but before calculated fields, lookups, event types and tags. You cannot alias a calculated field, an event type, a tag, or a lookup-added field. | 10-cim.md, ## Result contract |
| T-10-09 | 10.0 | `datamodelsimple` is a core SPL command, and `datamodel` is something the CIM add-on installs. | `datamodel`, `tstats`, `pivot` and `from` are core SPL. `datamodelsimple` is the custom command shipped inside `Splunk_SA_CIM` and does not exist without the add-on. | 10-cim.md, ### datamodelsimple (custom command from the CIM add-on) |
| T-10-10 | 10.0 | CIM expected values are enforced, so an event with `action=denied` in the Authentication model is rejected. | Expected values are "not exhaustive or exclusive" and nothing rejects an out-of-spec value. The consequence is silent: Enterprise Security content looking for `action=failure` simply will not match. | 10-cim.md, ### Field classification |
| T-10-11 | 10.0 | A Windows service being stopped belongs to the Application State model. | Application State is deprecated. Endpoint's Services dataset, tagged `service` and `report`, is the current answer. Endpoint superseded Application State in 4.12.0. | 10-cim.md, ### The CIM 8.6 data model catalogue |
| T-10-12 | 10.0 | A child dataset only needs its own tag. | Child datasets inherit the parent constraint. The Web model's Proxy dataset requires `web` and `proxy`. The Endpoint model's Filesystem dataset requires `endpoint` and `filesystem`. Adding only the child tag leaves the event out of both datasets. | 10-cim.md, ### The CIM 8.6 data model catalogue |
| T-10-13 | 10.0 | Clone a CIM data model before extending it, to keep the original for record keeping. | The docs state that cloning the data model and keeping the original is not considered a best practice. Add the custom field to the model itself, or better, avoid extending at all if a field mapping will do. | 10-cim.md, ## Decision rules |
| T-10-14 | 10.0 | The CIM Add-on and the CIM Add-on Builder are the same thing. | The Splunk Common Information Model Add-on (app 1621, `Splunk_SA_CIM`) supplies the models. The Splunk Add-on Builder is a separate app whose "Map to data model" step helps generate mapping knowledge objects for a new sourcetype, and it requires the CIM add-on to map to CIM models. Only the first is on the blueprint. | 10-cim.md, ## What it is |
| T-10-15 | 10.0 | `cim_summary` is the index the add-on uses. | `cim_summary` is deprecated and has been removed. The index that matters now is `cim_modactions`, which supports common action model alerting and auditing. | 10-cim.md, ## Result contract |
| T-10-16 | 10.0 | the catalogue has a published size you are meant to know. | the 8.6 docs state no total for either, the catalogue has grown across releases, and any number in an option is inherited from older material. Judge the option on the model names it uses, not on its arithmetic. | 10-cim.md, ## Traps |
| T-10-17 | 10.0 | An option says the add-on ships tsidx summary files, custom visualizations, or a library of CIM-compliant security dashboards. | none of those. Column stores exist only once acceleration is enabled, which it is not on a fresh install, and the one dashboard shipped is Data Model Audit for acceleration health. The polished CIM dashboards come from Enterprise Security and the PCI Compliance app. | 10-cim.md, ## Traps |
| T-10-18 | 10.0 | A statement credits the knowledge manager with using the CIM to create data models, or says the CIM builds your field extractions for you. | the models ship as JSON in `Splunk_SA_CIM/default/data/models` and are not built locally, and the knowledge manager creates the objects that map data into them: extractions, aliases, calculated fields, lookups, event types, and tags. | 10-cim.md, ## Traps |
| T-10-19 | 10.0 | A question asks which knowledge objects the CIM uses to normalize data and offers search macros or workflow actions. | the documented set is field aliases, field extractions, and lookups, alongside the event types and tags that admit events to a dataset, plus calculated fields for derived values. The shipped macros report acceleration status and filter data out of models, so "the add-on includes macros" is true while "macros normalize . | 10-cim.md, ## Traps |
| T-CIM-01 | CIM (10.0) | The child tag is the whole constraint, so an event tagged `proxy` lands in the Web model's Proxy dataset. | Proxy needs `web` and `proxy`, because child datasets inherit the parent constraint. Same shape for Endpoint Filesystem, Malware_Attacks, and Change Network_Changes. | cim-data-models.md, ## 1. Master catalogue |
| T-CIM-02 | CIM (10.0) | `action=denied` or `action=deny` is correct for a Network Traffic firewall event. | Network Traffic prescribes `allowed`, `blocked` and `teardown`. Splunk does not reject `denied`, it just never matches ES content. Same trap with `action=failed` instead of `failure` in Authentication. | cim-data-models.md, ### Allowed values of `action`, by model |
| T-CIM-03 | CIM (10.0) | `action=teardown` or `action=deferred` is valid on an Intrusion Detection event. | Intrusion Detection allows only `allowed` and `blocked`. `teardown` belongs to Network Traffic and `deferred` to Malware_Attacks. | cim-data-models.md, ### Allowed values of `action`, by model |
| T-CIM-04 | CIM (10.0) | Malware_Operations requires `action` and `signature`. | It requires `dest`, `signature_version` and `vendor_product`, and has no `action` field at all. It describes the health of the protection deployment, not a detection. | cim-data-models.md, ### 3.5 Malware |
| T-CIM-05 | CIM (10.0) | A Network Traffic mapping is complete with `src`, `dest`, `transport`, `action` and ports. | The required set also contains `app`, `dvc`, `src_zone`, `dest_zone` and `src_translated_ip`, which are required even with no segmentation and no NAT. | cim-data-models.md, ### 3.2 Network Traffic |
| T-CIM-06 | CIM (10.0) | `action` records whether a change succeeded. | In Change, `action` is what was attempted and `status` carries `success` or `failure`. Only in Authentication does `action` itself carry the outcome. | cim-data-models.md, ### 3.4 Change |
| T-CIM-07 | CIM (10.0) | Web `status` is a number, so `\| where Web.status > 400` works. | CIM types Web `status` as a string. `\| search Web.status=4*` or an explicit `tonumber()` is the honest form. | cim-data-models.md, ### 3.3 Web |
| T-CIM-08 | CIM (10.0) | An event missing a required field is excluded from the data model. | Tags decide membership, fields do not. The event sits in the dataset with a null column and disappears only from searches that use that field. Nothing warns. | cim-data-models.md, ## 2. Field classification semantics |
| T-CIM-09 | CIM (10.0) | Required fields are the ones flagged `recommended=true` in the model JSON. | Two separate marks. `recommended=true` flags recommended fields; required is marked in the 8.6 tables as "required for pytest-splunk-addon". Many fields carry both. | cim-data-models.md, ## 2. Field classification semantics |
| T-CIM-10 | CIM (10.0) | `FROM datamodel=Network Traffic.All_Traffic` is valid `tstats` syntax. | The reference uses the internal name, so spaces become underscores: `Network_Traffic`, `Intrusion_Detection`, `Network_Resolution`. The spaced display name belongs in the UI, not the search. | cim-data-models.md, ## 6. Validation recipes |
| T-CIM-11 | CIM (10.0) | `\| from datamodel.Web.Web` or `\| from datamodel:Web:Web` is the `from` syntax. | The syntax is `\| from datamodel:<model>.<dataset>`, colon after `datamodel`, dot between model and dataset. Quotation marks only when a name contains spaces. | cim-data-models.md, ## 6. Validation recipes |
| T-CIM-12 | CIM (10.0) | The Indexes allowlist must be set before a CIM model returns data, or new indexes join it automatically. | The default is all indexes, so the allowlist is purely a performance control, and new indexes must be added by hand. | cim-data-models.md, ## 5. The `cim_*_indexes` macro convention |
| T-CIM-13 | CIM (10.0) | A Windows service state change belongs equally to Endpoint or Change. | Endpoint's Services dataset (`service`,`report`) records observed service state on a client; Change's Endpoint_Changes (`change`,`endpoint`) records an administrative modification. Observation goes to Endpoint, administration to Change. | cim-data-models.md, ## 1. Master catalogue |
| T-CIM-14 | CIM (10.0) | CIM Validation (S.o.S.) is one of the documented models, or Compute Inventory is current. | The Data models section documents twenty-six models, two deprecated. CIM Validation (S.o.S.) is documented on the validation page, and Compute Inventory is the legacy slug for Inventory. | cim-data-models.md, ## 1. Master catalogue |
| T-CIM-15 | CIM (10.0) | A DHCP lease or VPN connection belongs to Network Traffic. | Network Sessions covers DHCP (`network`,`session`,`dhcp`) and VPN (`network`,`session`,`vpn`). A VPN tunnel's payload flows are Network Traffic; the tunnel establishment is Network Sessions. | cim-data-models.md, ## 1. Master catalogue |
| T-CIM-16 | CIM (10.0) | `bytes` is unrelated to `bytes_in` and `bytes_out`. | All three are required in Web and recommended in Network Traffic, and CIM expects `bytes` to be the sum. `EVAL-bytes = bytes_in + bytes_out` is the normal way to satisfy it. | cim-data-models.md, ## 7. Cross-model field quick reference |
| T-KO-01 | KO (4.0, 5.0) | The sequence is extraction, alias, calculated field, lookup, event type, tag, full stop. | The 10.4 docs list nine operations. Field filters is number 1, and field extraction is three separate operations: inline `EXTRACT-` at 2, transform-based `REPORT-` at 3, automatic key-value via `KV_MODE` at 4. The six-step version is items 2 through 9 collapsed. | knowledge-object-precedence.md, ## The documented sequence, nine operations |
| T-KO-02 | KO (5.0) | Calculated fields are evaluated before field aliases, because eval feels more primitive. | Field aliasing is stage 5 and calculated fields are stage 6. "Calculated fields come after field aliasing but before lookups." A calculated field can use an alias; an alias cannot use a calculated field. | knowledge-object-precedence.md, ## The documented sequence, nine operations |
| T-KO-03 | KO (5.0) | `FIELDALIAS-x = my_eval_field AS foo` produces `foo`. | The source field does not exist at stage 5, so nothing is aliased. Worse, since 7.2.4, if the alias field already exists on the event and the source field does not, the alias field is removed from the event. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-04 | KO (5.0) | `EVAL-margin = price - cost` works when `price` comes from an automatic lookup. | Lookups are stage 7 and calculated fields are stage 6, so `price` is null when the eval runs. Calculated fields cannot reference lookups, event types or tags. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-05 | KO (6.0) | `search = tag=authentication` is a valid event type definition. | "Search strings that define event types cannot reference tags, because event types are always processed and added to events before tags." The event type will match nothing. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-06 | KO (6.0, 10.0) | Tags are only for raw extracted fields, so they cannot apply to event types. | Tags run last and can be applied to any field-value pair including `eventtype`, whether the field came from index time, search time, an event type, a lookup or a calculated field. Tagging event types is the standard CIM normalization pattern, and the Splunk Web event type form has a Tag(s) field. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-07 | KO (7.0) | Macros are applied after field extraction, or macros are a search-time knowledge object like the others. | Macros are textual substitution into the search string during parsing, before any event is fetched and before stage 1. The sequence page does not list them. limits.conf bounds recursion with `max_macro_depth`, default 100. | knowledge-object-precedence.md, ## Where macro expansion sits, and why it is not in the list |
| T-KO-08 | KO (4.0) | Adding `EXTRACT-` means old events do not get the field until you reindex. | Search-time objects are evaluated per search against existing data, so they apply retroactively to every event already on disk. Only index-time changes such as `TRANSFORMS-`, line breaking, timestamping, host assignment and source type assignment require reindexing. | knowledge-object-precedence.md, ### Why index-time changes need reindexing and search-time ones do not |
| T-KO-09 | KO (5.0) | `LOOKUP-a` outputs `dept`, so `LOOKUP-b` can key on `dept`. | "Splunk software does not support nested automatic lookups." Both configurations belong to the same stage. Chain them explicitly with the `lookup` command in SPL, where pipeline order is yours. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-10 | KO (5.0) | `FIELDALIAS-a = clientip AS ip` then `FIELDALIAS-b = ip AS src` gives you `src`. | Aliases are one operation, processed in lexicographical order by class name, and chaining is not a documented capability. When two alias configurations write the same destination field the lexicographically last one wins. The documented way to build a field from several sources is a calculated field using `coalesce`, `mvappend` or `mvdedup`. | knowledge-object-precedence.md, ## Ordering inside a single stage: lexicographical order |
| T-KO-11 | KO | Precedence is one concept, so `local` beating `default` somehow determines whether a lookup can see an alias. | They are unrelated. The sequence decides in what order objects are applied to events. Configuration file precedence decides which copy of a duplicated setting survives the merge. | knowledge-object-precedence.md, ## Configuration file precedence is a different thing |
| T-KO-12 | KO | Apps are always evaluated in lexicographical order. | The global context uses lexicographical order of app directory names; the app or user context uses reverse-lexicographical order. The two contexts resolve app collisions in opposite directions. | knowledge-object-precedence.md, ## Configuration file precedence is a different thing |
| T-KO-13 | KO | Lexicographical order is alphabetical and numeric, so `EXTRACT-aaa` runs before `EXTRACT-ZZZ` and `EXTRACT-9` before `EXTRACT-10`. | Uppercase sorts before lowercase, so `ZZZ` runs first, and numbers sort on the first digit, so 10, 100, 70, 9 is the sorted order. Pad numeric prefixes if you need real ordering. | knowledge-object-precedence.md, ## Ordering inside a single stage: lexicographical order |
| T-KO-14 | KO (5.0) | `EVAL-a` can feed `EVAL-b` if you name them in the right order. | "All `EVAL-<fieldname>` configurations within a single `props.conf` stanza are processed in parallel instead of sequentially." Lexicographical order does not save you. A calculated field also overrides an extracted field of the same name even when the eval returns null, which is why `coalesce` is the documented guard. | knowledge-object-precedence.md, ## Ordering inside a single stage: lexicographical order |
| T-KO-15 | KO | `OUTPUT` and `OUTPUTNEW` are interchangeable, or `OUTPUTNEW` means only new rows in the lookup table. | With `OUTPUT`, output fields that already exist in the event are overwritten. With `OUTPUTNEW`, Splunk adds only the output fields that are new to the event, leaving existing values alone. The same distinction exists for field aliases as `AS` versus `ASNEW`. | knowledge-object-precedence.md, ## Which configuration file backs each stage |
| T-KO-16 | KO (6.0) | Event type priority 10 is best because bigger is better. | "1 is the highest priority and 10 is the lowest priority." Priority determines the display order of matching event types in the expanded event, and which colour shows when several matching event types define one. Splunk processes event types by priority first, then lexicographically. The docs publish the 1 to 10 range but no default value for `priority`. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-17 | KO (4.0) | `rename` in props.conf is an index-time change and requires reindexing, and the old stanza's extractions keep working. | "The renaming of source types occurs only at search time." The original source type moves to `_sourcetype`. And "Data from a renamed source type uses only the search-time configuration for the target source type", so extractions defined under the original stanza name stop applying. | knowledge-object-precedence.md, ## Where index-time processing sits |
| T-KO-18 | KO | Event type matching happens before field extraction, because the "Index time versus search time" page prints it in that order. | That page is a category list of which activities happen in which phase. The ordered authority is "The sequence of search-time operations", where event types are stage 8 and extraction is stages 2 through 4. | knowledge-object-precedence.md, ## Where index-time processing sits |
| T-KO-19 | KO (5.0) | Aliasing is purely additive and never removes anything. | "An alias does not replace or remove the original field name", so the source field survives. But the destination behaves conditionally: since 7.2.4, if the source field is absent and the alias field is present, the alias field is removed from the event. Use `ASNEW`, or the Splunk Web Overwrite field values checkbox, to control this. | knowledge-object-precedence.md, ## The consequences table |
| T-KO-20 | KO (4.0) | `punct` and `linecount` are search-time extractions, or `_time` is derived at search time. | Default fields are added at index time. Internal fields are `_raw`, `_time`, `_indextime`, `_cd`, `_bkt`. Default fields are `host`, `index`, `linecount`, `punct`, `source`, `sourcetype`, `splunk_server`, `timestamp`. Default datetime fields are `date_hour`, `date_mday`, `date_minute`, `date_month`, `date_second`, `date_wday`, `date_year`, `date_zone`. | knowledge-object-precedence.md, ### Default fields |
| T-RX-01 | RX (4.0) | `"\[(?<ts>.*)\]"` extracts the timestamp from `[01/Sep/2022:18:23:07] VendorID=5037`. | `.*` is greedy and runs to the end of the line before backtracking to the last `]`, so on any event containing a second closing bracket the field swallows everything in between. Use the lazy form `.*?` or a negated class `[^\]]*`, which cannot cross the delimiter at all. | regex-for-splunk.md, ## Compact syntax table |
| T-RX-02 | RX (4.0) | `"host=(?<h>www1.example.com)"` and `"screen?productId=(?<p>\w+)"` say what they look like they say. | An unescaped `.` matches any character, so `www1.example.com` also matches `www1xexample!com`, and an unescaped `?` makes the preceding character optional, so `screen?` matches `scree` followed by an optional `n`. Write `www1\.example\.com` and `screen\?`. | regex-for-splunk.md, ## Compact syntax table |
| T-RX-03 | RX (4.0) | `"user (?<user>.*) from"` is fine because the pattern ends at ` from`. | On a line containing a second occurrence of ` from` the greedy `.*` matches through the first one. `[^ ]*` or `\S+` is the correct token grab, cannot cross the space, and needs no backtracking. Prefer a negated class to a dot-star in every Splunk extraction. | regex-for-splunk.md, ## Compact syntax table |
| T-RX-04 | RX (4.0) | Any capture group name works because it is just a label. | Two rule sets apply. The Field Extractor states that "Field names must start with a letter and contain only letters, numbers, and underscores." At the configuration-file level, "Valid characters for field names are a-z, A-Z, 0-9, . , :, and _" and "Field names cannot begin with 0-9 or _". So `(?<src-ip>...)`, `(?<2nd_ip>...)` and `(?<_ip>...)` are all wrong. | regex-for-splunk.md, ## What the Field Extractor actually generates |
| T-RX-05 | RX (4.0) | `^` and `$` always mean the start and end of the event. | They mean the start and end of whatever `rex` is reading. `\| rex field=uri_query "^(?<first_param>[^&]+)"` anchors at the first character of `uri_query`; drop the `field=` and the same pattern anchors on `_raw`. Generated patterns carry a `^`, so moving one from an FX extraction into `\| rex field=something` breaks it. | regex-for-splunk.md, ## rex, the extracting command |
| T-RX-06 | RX (4.0) | Since `status=Failed` matches `failed` in the search bar, `rex "Failed password"` matches `failed password`. | Search-term matching on field values is case insensitive; regular expression matching is case sensitive. `\| rex "(?<u>invalid user \S+)"` will not fire on `Invalid user`. Prefix the pattern with `(?i)`, which is exactly why Splunk's own generators put `(?i)` at the front of what they produce. | regex-for-splunk.md, ## The flavour |
| T-RX-07 | RX (4.0) | `rex` and `regex` are the same command because both take a PCRE. | `rex` extracts and never changes the row count; `regex` filters and never creates a field. A question that says "500 events in, 120 match" is testing this: `rex` emits 500, `regex` emits 120. | regex-for-splunk.md, ## regex versus rex |
| T-RX-08 | RX (4.0) | `\| rex "port (\d+)"` creates a field, perhaps called `1`. | `rex` creates fields from named groups only, so that command runs, matches, and produces nothing. In a `transforms.conf` transform an unnamed group is usable, but only because `FORMAT = $1::$2` supplies the naming that the regex left out. | regex-for-splunk.md, ## rex, the extracting command |
| T-RX-09 | RX (4.0) | The `P` in `(?P<name>...)` is a typo, or Python syntax that Splunk rejects. | The docs list `(?P<field_name>...)` and `(?<field_name>...)` side by side as valid. Both create the same field. The generated expressions you will be shown on the exam are likelier to carry the `P`. | regex-for-splunk.md, ## The flavour |
| T-RX-10 | RX (4.0) | Quoting inside the pattern is free. | The pattern is a double-quoted SPL string, so an inner double quote must be written `\"`, and a literal parenthesis in the data must be escaped as `\(` or it opens a group. Both mistakes produce a search that either errors or silently matches nothing. | regex-for-splunk.md, ## rex, the extracting command |
| T-RX-11 | RX (4.0) | `examples="port 3351"` is a friendly way to say "the port number". | `erex` captures whatever you show it. The documented output for that example is `(?P<port>\w+\s+\d+)`, whose values are the string `port 3351`, label included. Give it `3351` if you want digits, and add `counterexamples=` to steer it off the values it should leave alone. | regex-for-splunk.md, ## erex, the generator |
| T-RX-12 | RX (2.0, 4.0) | `uri_path=*cart*`, `like(user,"admin%")` and `match_type = WILDCARD(host)` are PCRE under the hood. | The `search` asterisk is a glob over terms, `like()` uses `%` and `_` from SQL, and lookup `WILDCARD()` is glob matching on `*` inside the lookup table values. Only `rex`, `regex`, `erex`, `match()`, `replace()`, `mvfilter()` with `match()`, `mvfind()`, and the extraction settings in `props.conf` and `transforms.conf` take PCRE. | regex-for-splunk.md, ## Where regex turns up outside rex |
| T-TM-01 | TM (1.0) | `-1d@d` means twenty-four hours ago. | The offset applies first, giving this time yesterday, then the snap rounds down to the start of that day. At 14:37 on Wednesday it resolves to Tuesday 00:00:00, which is over 38 hours back. Twenty-four hours ago is `-24h` or bare `-1d`. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-02 | TM | `@w` snaps to Monday because the working week starts on Monday. | The week snap lands on Sunday. `@w`, `@w0` and `@w7` are all Sunday, `@w1` is Monday and `@w6` is Saturday. A business-week range must say `@w1` explicitly. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-03 | TM | Snapping rounds to the nearest boundary. | Snapping always rounds down, to the latest time that is not after the value, and zeroes every smaller unit. `-30m@h` at 14:50 gives 14:00 and can never give 15:00. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-04 | TM | "Yesterday" is `earliest=-1d`. | `Yesterday` is `earliest=-1d@d latest=@d`, a fixed calendar day. `Last 24 hours` is `earliest=-24h latest=now`, a rolling window. Only the second one moves with the clock. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-05 | TM | `-1mon` from 31 March gives 31 February, or gives thirty days earlier. | `mon` is a calendar unit, so `-1mon` from 22 July is 22 June. From 31 March there is no 31 February, so the result is clamped into February. The deterministic form is `-1mon@mon`, which always lands on the first of the previous month at 00:00:00. Carries an open `[verify]`: the docs do not state the clamping rule. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-06 | TM | Snapping is absolute, so `@d` is the same instant for everybody. | `_time` is absolute, but the snap is evaluated in the searching user's time zone. `@d`, `@w`, `@mon` and the `Today` and `Since 00:00:00` presets produce different epoch instants for users in different zones and therefore different result sets. `earliest=-24h` does not have this problem. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-07 | TM | `m` means month. | `m` is minutes. Months are `mon`, `month` or `months`. `-3m` is three minutes ago, and a question that reads `earliest=-6m` next to answer options about half a year is testing exactly this. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-08 | TM | In `-2h@h` the snap runs first. | Left of `@` runs before right of `@`. An offset written after the snap unit runs last, which is why `-1d@d+3h` (03:00 yesterday) and `@d-2h` (22:00 the previous night) are different instants and neither equals `-1d@d`. | time-modifiers.md, ## 3. Relative time syntax in full |
| T-TM-09 | TM | `latest` is inclusive, and omitting it means "no upper bound". | `latest` is exclusive at the boundary, so `latest=@d` stops just before midnight and does not double-count the first event of today. Omitting `latest` sets it to `now()`, which excludes nothing in practice but is not the same as unbounded. | time-modifiers.md, ## 2. The four time modifiers |
| T-TM-10 | TM | `_index_earliest=-1h@h` on its own returns everything indexed in the last hour. | Index-time modifiers filter `_indextime` but do not widen the event-time window: "your search must also have an event-time window which will retrieve the events". Backfilled data indexed today with an event time of last week needs `earliest=-7d` as well. | time-modifiers.md, ## 2. The four time modifiers |
| T-TM-11 | TM | `date_hour` is the hour of the event in my time zone. | The `date_*` fields are parsed from the timestamp as written in the raw event, exist only when the source system supplied timestamp information, and do not follow the viewing user's time zone preference. Events with an inferred timestamp have no `date_*` fields at all. Use `strftime(_time, "%H")`. | time-modifiers.md, ## 1. Where time lives on an event |
| T-TM-12 | TM (1.0) | `bin _time span=1h \| stats count` is just a longer way of writing `timechart span=1h count`. | `stats` emits nothing for empty buckets and produces long-format rows with no split limit; `timechart` fills gaps, enforces the search range, keeps partial end buckets, produces wide format, and caps split values at the top ten with the remainder in `OTHER`. | time-modifiers.md, ### bin plus stats versus timechart span |
| T-TM-13 | TM (1.0) | A `timechart` with no `span` always produces the same granularity. | With `span` omitted it falls back to a maximum of `bins=100` and to a preset-dependent default (10 seconds for Last 15 minutes, 30 minutes for Last 24 hours, 1 day for Last 7 days, 1 month for Previous year), so moving the picker changes the row count without touching the SPL. If both are supplied, `span` wins and `bins` is ignored. | time-modifiers.md, ### When span is omitted on timechart |
| T-TM-14 | TM (1.0) | `span=1y` and `span=1d@d` are valid on `timechart`. | The documented span-length timescales are seconds, minutes, hours, days, weeks, months, quarters and subseconds; years are not listed, so use `span=12mon`. Snap-to spans are supported only for weeks, as in `span=w@w1`. Separately, `aligntime` is ignored when the span is in days, months or years. | time-modifiers.md, ## 5. bin, bucket and span |
| T-TM-15 | TM (2.0) | `strftime()` will format any epoch value, and `strptime()` will normalise `_time`. | `strftime()` requires seconds, so a millisecond field must be divided by 1000 first or you get a date in the year 55000. `strptime()` on `_time` does nothing, because `_time` is already epoch; it also needs a day component in the string and rejects dates before 1 January 1971. | time-modifiers.md, ## 4. Time functions in eval |
| T-TM-16 | TM | An all-time real-time search shows every matching event indefinitely. | Results accumulate rather than ageing out of a window, but the display is a circular buffer holding roughly the most recent 1,000 results. Real-time search also needs the `rtsearch` capability (held by `admin`, `sc_admin` and `power`), and indexed real-time search, the lower-impact variant, is off by default in Splunk Enterprise. | time-modifiers.md, ## 6. Real-time versus historical |
| T-TM-17 | TM (2.0) | `now()` and `time()` are the same function. | `now()` is fixed for the whole search, set at search start or at the scheduled run time, so every row gets the same value. `time()` is evaluated per result with microsecond resolution, so it varies row to row. Use `now()` for anything you intend to compare across rows. | time-modifiers.md, ## 4. Time functions in eval |

## The top twenty

Ranked by expected value on the exam: section weight multiplied by how reliably practitioners get this one wrong. Section 3.0 is 15 percent of the paper and is the single densest source of default-value questions, so it takes five slots. Section 10.0 carries 10 percent with no official course behind it, so anything you know only from ES habit is a risk, and it takes two. Section 1.0 is 5 percent and takes one.

**1. T-03-01, the `transaction` defaults.** `maxspan` and `maxpause` both default to `-1`, which means the constraint is deactivated, not "unset with some sensible fallback". Only `maxevents` has a positive default, `1000`. The exam builds this as a four-option question where three options are plausible durations. The reasoning that saves you is that a default duration would silently break every unconstrained `transaction` in the product, so there cannot be one. A negative value on any of the three deactivates that constraint (T-03-14), and `1000` is not a product ceiling (T-03-19): `maxopentxn` at 5000 and `maxopenevents` at 100000 are the real ones, and they evict rather than truncate.

**2. T-KO-02, aliases before calculated fields.** Field aliasing is stage 5, calculated fields are stage 6, lookups are 7, event types 8, tags 9. Everything downstream can see everything upstream and nothing upstream can see anything downstream. That one sentence generates the whole family: a calculated field can consume an alias but an alias cannot consume a calculated field (T-05-06, T-KO-03); a calculated field cannot read a lookup output (T-05-05, T-KO-04); an event type definition cannot reference a tag (T-KO-05); a tag can be applied to an event type (T-KO-06). The exam asks it in every one of those disguises across sections 4, 5, 6 and 10, so it is worth more than its nominal weight. If you learn nothing else, learn the number of each stage.

**3. T-06-01, a tag attaches to a field-value pair.** Not to an event, not to a field. You cannot tag "this event". You tag `host=web01`. The event only carries the tag because it contains that pair. The distractor set almost always contains "a tag is applied to a field" beside "a tag is applied to a field-value pair", and the two look close enough at speed that people take the shorter one. The corollary that makes the fact stick is T-06-14: tagging never changes the value, which is why tagging is the standard fix for a host that was renamed mid-history. Tag both old and new values with one tag and search the tag.

**4. T-03-10, `transaction` produces `eventcount`, not `count`.** A question shows `transaction clientip` followed by `where count>5` and asks why there are no results. The answer is that `count` is the `stats` field and `transaction` never creates it. This is a good question because it punishes exactly the habit an experienced practitioner has: `count` is so ubiquitous that you stop reading it. Remember the four fields `transaction` adds: `duration` in seconds (T-03-03), `eventcount`, `closed_txn`, and `_time` taken from the earliest member event (T-03-04).

**5. T-05-01, an alias adds a name, it does not rename.** After `FIELDALIAS-x = clientip AS src_ip`, both `clientip` and `src_ip` work as search terms. The docs are explicit that an alias "does not replace or remove the original field name". The thing that actually removes the old name is the `rename` command, and that lasts one search and operates downstream of all nine search-time stages (T-05-02). One caveat that catches people who overlearn the additive rule: since 7.2.4, if the source field is missing and the alias field exists, the alias field is removed from the event (T-KO-19).

**6. T-08-01, Action type has two values.** `link` and `search`. Not three. GET and POST are both `link` actions, separated by `link.method`. The exam offers "GET, POST, Search" as an answer set constantly because the documentation itself talks about three types of workflow action in prose while the configuration exposes two values for `type`. Hold both models: three conceptual kinds, two values of `type`, and the discriminator between the first two is `link.method`, which defaults to `get` (T-08-03).

**7. T-03-12, `first`/`last` against `earliest`/`latest`.** `first()` returns the first value in processing order, and because search results arrive newest first, that is usually the most recent event, not the oldest. `last()` returns the oldest. If you want chronological semantics you must use `earliest()` and `latest()`, which read `_time`. The trap works because the English words point the opposite way from the behaviour. Say it to yourself as "first means first seen, and the first thing you see is the newest".

**8. T-04-01, extractions are search-time and retroactive.** Saving a field extraction in the Field Extractor writes a search-time configuration. It applies immediately to every event already on disk, needs no reindex and no restart. Candidates who have done real ingest work are the ones who get this wrong, because they associate "new field" with "new parsing". The clean split (T-KO-08): only `TRANSFORMS-`, line breaking, timestamp assignment, host assignment and source type assignment are index-time and therefore need reindexing. Everything in sections 4, 5 and 6 is search-time and retroactive.

**9. T-03-13, `values()` against `list()`.** `values()` returns the distinct values, lexicographically ordered, with no documented cap. `list()` returns every value in event order and stops at 100. The 100 is the examinable number and the one people do not know. When a question describes a scenario with a high-cardinality field and asks why the result set is incomplete, `list()` is the answer. The parallel inside `transaction` is `mvlist` (T-03-07), which defaults to `false` and gives you the unique-alphabetical rendering, with `mvlist=true` giving arrival order.

**10. T-07-02, the number in the macro name is the argument count.** A macro that takes two arguments is stored under the name `mymacro(2)` and invoked as `` `mymacro(foo,bar)` ``. The stored name carries a count; the call carries values. The related fact is that `[foobar]` and `[foobar(1)]` are genuinely different objects (T-07-11), so calling `` `foobar(x)` `` when only `[foobar]` exists is an error rather than a call with an ignored argument. And the Arguments form field holds bare names, `val, rate`, while the Definition references them as `$val$` and `$rate$` (T-07-03).

**11. T-10-03, tags are what admit an event into a CIM dataset.** Renaming your fields to `src`, `dest` and `action` does nothing on its own. The event dataset's constraint is a tag expression, so without `tag=authentication` on the event the Authentication model never sees it. Compliance is tags plus populated required fields, both. The follow-on (T-10-12) is that a child dataset inherits the parent constraint, so the Web model's Proxy dataset needs `web` and `proxy`, and tagging only `proxy` leaves the event out of both.

**12. T-09-06, Required is a filter.** Marking a field Required on a data model dataset is not documentation and not form validation. It acts as an extra constraint: any event lacking that field is removed from the dataset. Mark a sparse field Required and your dataset silently shrinks. Contrast with Hidden (T-09-07), which only suppresses the field from Pivot users defining a report and changes neither the dataset nor SPL access. Required filters events; Hidden filters what one UI shows you.

**13. T-02-03, `NOT field=value` against `field!=value`.** `status!=404` requires the field to exist and to hold something other than 404, so events with no `status` are dropped. `NOT status=404` is set complement over the whole result set, so events with no `status` survive. The two extreme cases make the rule memorable: `NOT field=*` returns exactly the events where the field is null or undefined, and `field!=*` returns nothing at all, ever. Pair this with T-02-02: `search` evaluates OR before AND, while `eval` and `where` evaluate AND before OR, so the same written expression means different things in the two contexts.

**14. T-05-07, `EVAL-` configurations in a stanza run in parallel.** Two calculated fields in the same props.conf stanza cannot chain. Write `EVAL-response_time = response_time/1000` and then `EVAL-bitrate = bytes*1000/response_time` and the second still reads the original extracted millisecond value. The reason this is dangerous rather than merely wrong is that it produces a plausible number, not a null and not an error, so nothing in the UI tells you. Lexicographical naming does not fix it (T-KO-14). The only fix is one expression that does both steps, or a second stanza.

**15. T-03-05, `closed_txn` and the `endswith` omission.** `closed_txn` is set to `1` only when one of `maxevents`, `maxspan`, `maxpause` or `startswith` is specified. `endswith` is not on that list, which reads as an inconsistency and is exactly why it is examinable. If none of the four is present, every transaction comes out with `closed_txn=0`. Keep it distinct from the orphan marker: `keepevicted=true` surfaces open transactions flagged `closed_txn=0`, while `keeporphans=true` surfaces events that joined nothing, flagged `_txn_orphan=1` (T-03-06, T-03-20). Both options default to false.

**16. T-08-05, the workflow action field list is a conjunction.** `fields = src_ip, dest_ip` means the action appears only on events carrying both. Not either. The documented example spells it out: `*, ip_client, ip_server` applies to all fields in the event if `ip_client` and `ip_server` are both present. The companion default is that `fields` defaults to `*` (T-08-04), so an action you never scoped appears everywhere, and `display_location` defaults to `both` (T-08-12), so it appears in the event menu and every field menu.

**17. T-10-02, CIM acceleration is off by default.** Every data model in the CIM add-on ships with acceleration disabled. You turn it on per model on the CIM Setup page. The consequence the exam likes is that a `tstats summariesonly=t` search against a freshly installed CIM model returns zero rows, which reads like a broken search and is actually correct behaviour. The Apress answer key asserts the opposite, so if you revised from that book you have this backwards. Also on the setup page: the Indexes allowlist defaults to all indexes (T-10-04), not to empty.

**18. T-06-05, priority 1 beats priority 10.** Event type priority is an integer 1 through 10 and 1 is best. The docs' own example gives `critical_disk_error` priority 3 and `all_system_errors` priority 7, and describes 3 as the better value. Priority does two jobs, not one (T-06-06): it orders matching event types in the display, and it decides which single colour renders when several matching event types define one. Event types with no priority set sort alphabetically below all those that have one.

**19. T-09-11, `summariesonly` defaults to false.** By default `tstats` runs over accelerated and unaccelerated data models alike, so it works whether or not you turned acceleration on. Setting `summariesonly=true` restricts the search to summarized data, which is faster and is incomplete for any time outside the Summary Range. The frequent misreading is that `tstats` is a summaries-only command by nature. It is not. Sitting beside it: `strict_fields` on the `datamodel` command defaults to `true` (T-09-12), which is why `datamodel M D search` shows you fewer fields than the dataset defines.

**20. T-01-01, `limit` caps columns.** On `chart`, `limit` defaults to `top 10`, is only valid when a column-split is specified, and limits the number of columns. On `timechart`, `limit` defaults to `top10` distinct split-by values, and the number of rows comes from `span` or `bins`. Nothing about `limit` on either command constrains rows. `limit=0` means all, not none (T-01-17). And on `top` and `rare`, `limit` genuinely does control the number of rows returned, which is the source of the confusion.

Just below the line, and worth a glance if you have time: T-03-09 (`stats` outperforms `transaction`, which is centralized streaming and search-head only), T-08-02 (`link.target` defaults to `blank`, a new window), T-02-02 (Boolean precedence differs between `search` and `where`), T-04-09 (`max_match` defaults to 1 and `max_match=0` means unlimited), T-07-01 (backticks, not quotation marks), T-09-01 (the data model feeds Pivot, not the reverse).

## Confusable pairs index

Alphabetical by the first item in the pair. One line each, phrased as the discriminator you need in the moment.

| Pair | Discriminator |
|---|---|
| `append` vs `union` vs `join` | `append` adds rows at the end and blocks a later `transaction`; `union` merges datasets; `join` matches on a key with `max=1` by default and a 50,000-row, 60-second subsearch ceiling. |
| Boolean precedence in `search` vs in `eval`/`where` | `search` evaluates OR before AND; `eval` and `where` evaluate AND before OR. `search` has no XOR; the other two do. |
| Calculated field vs lookup (ordering) | Calculated field is stage 6, lookup is stage 7. The calculated field can never see the lookup output; the lookup can key on the calculated field. |
| `case()` vs `coalesce()` vs `if()` | `case()` takes condition-value pairs and returns NULL if none is true; `coalesce()` takes plain values and returns the first non-null; `if()` takes exactly three arguments. |
| `chart` vs `timechart` vs `stats` output shape | `stats` keeps every BY field as a column, one row per combination; `chart` turns the first BY field into rows and the second into column headers; `timechart` fixes rows to `_time` and allows exactly one BY field as columns. |
| `closed_txn` vs `_txn_orphan` | `closed_txn=0` marks an evicted or never-closed transaction, surfaced by `keepevicted`; `_txn_orphan=1` marks an event that joined no transaction, surfaced by `keeporphans`. |
| Data model acceleration vs ad hoc acceleration | Persistent acceleration writes summaries to the indexers and survives; ad hoc acceleration lives in the search head dispatch directory and is deleted when you leave the Pivot Editor. |
| Data model acceleration vs report acceleration vs summary indexing | Acceleration of a model builds tsidx summaries on the indexers; report acceleration needs a transforming search with only streamable commands before the transform and 100,000 events in the covered hot bucket; summary indexing is a scheduled search you write that populates an index you nominate. |
| Dataset Name vs Dataset ID | Name accepts anything but asterisks and can be changed; ID is alphanumerics, underscores and hyphens only, no spaces, and is frozen once saved. |
| `dc()` vs `estdc()` | `dc()` is exact distinct count; `estdc()` is an estimate with a documented error bound, cheaper on high cardinality. |
| `eval` command vs calculated field | The `eval` command creates a field for one pipeline and its result is not usable in the base search; a calculated field creates it for every search against the scoped host, source or source type and is directly searchable before the first pipe. |
| Event dataset vs search dataset vs transaction dataset | Event datasets are constrained by a simple search with no pipes and can be accelerated; search datasets carry a full SPL search and lose acceleration if it is transforming; transaction datasets group events from existing datasets and do not benefit from acceleration. |
| Event type vs saved search vs report | A report is a saved search you run and it returns results; an event type is a classification applied automatically at search time and you never run it; a saved search is the underlying object a report is one presentation of. Reports may contain pipes and subsearches; event types may not. |
| `extract` vs `rex` vs `regex` | `extract` (alias `kv`) pulls key-value pairs from `_raw` only and creates fields; `rex` applies a named-capture regex to any field, defaulting to `_raw`, and creates fields; `regex` filters events and creates nothing. |
| Field alias vs `rename` vs `eval` | An alias adds a second name at stage 5 and keeps the original, permanently and for every search in scope; `rename` replaces the name in this result set only, after all nine stages; `eval` computes a new value into a new or existing field. |
| `fields` (workflow action) vs `display_location` | `fields` decides which events qualify, and every listed field must be present; `display_location` decides which menu the entry appears in. |
| `fieldformat` vs `eval` | `fieldformat` changes rendering only and later commands and `outputcsv` see the original value; `eval` changes the value itself. |
| `fillnull` vs `filldown` | `fillnull` writes one constant, default `0`, into every null cell and takes no wildcards; `filldown` copies the last non-null value from the row above and its field list does accept `*`. |
| `first()`/`last()` vs `earliest()`/`latest()` | `first()` and `last()` are processing order, so `first()` is usually the newest event; `earliest()` and `latest()` read `_time` and are chronological. |
| GET vs POST vs Search workflow action | All three set `type` to `link` or `search`: GET and POST are both `link` and differ only in `link.method`; a Search action sets `type = search` and runs SPL in Splunk. |
| `IN` operator vs `in()` function | The `IN` operator works in `search` and `tstats` and accepts `*` wildcards; the `in()` function works in `eval`, `where` and `fieldformat`, needs quoted values, accepts no wildcards, and under `eval` must be nested in `case`, `if` or `validate`. |
| Interesting Fields vs All Fields | Interesting Fields is a 20 percent display threshold against the events this search returned; All Fields is everything extracted. Neither affects what is searchable. |
| `link.target` vs `display_location` | `link.target` is which window the destination opens in, default `blank`; `display_location` is which Splunk menu the entry is drawn in, default `both`. |
| Macro backtick vs field single-quote | A macro reference is wrapped in backticks; a single quotation mark makes it an ordinary search term and nothing expands. |
| Macro name argument count vs invocation arguments | The stored name carries the count, `mymacro(2)`; the invocation carries the values, `` `mymacro(foo,bar)` ``. `[foobar]` and `[foobar(1)]` are different objects. |
| `maxspan` vs `maxpause` vs `maxevents` | `maxspan` is total transaction duration; `maxpause` is the longest gap between adjacent events; `maxevents` is the member count. First two default to `-1` (off), the third to `1000`. |
| `mvindex()` vs `substr()` index base | `mvindex()` and `mvfind()` count from 0 with `-1` as the last element; `substr()` counts from 1 with SQLite semantics. |
| Name vs Label (workflow action) | Name is the internal object name and becomes the stanza header; Label is the menu text and is the field that accepts `$field$` tokens. |
| `NOT` vs `!=` | `NOT field=value` keeps events with no `field` at all; `field!=value` requires the field to exist. |
| `null()` vs `nullif()` vs `isnull()` | `null()` takes no arguments and returns NULL; `nullif(a,b)` returns NULL when the two are equal, otherwise `a`; `isnull(x)` returns a Boolean and therefore cannot be assigned directly under `eval`. |
| `OUTPUT` vs `OUTPUTNEW` (and `AS` vs `ASNEW`) | `OUTPUT` overwrites an existing field of that name; `OUTPUTNEW` writes only when the field is new to the event. `ASNEW` is the alias equivalent. |
| `OVER` vs `BY` on `chart` | `OVER` names the row-split, which becomes the first column and the X-axis; `BY` names the column-split, which becomes the series. `chart X BY a b` equals `chart X OVER a BY b`. |
| Priority vs lexicographic order (event types) | Priority orders first and 1 is best; lexicographic order breaks ties and orders every event type that has no priority, below those that do. |
| Required vs Optional vs Hidden (data model fields) | Required filters out events lacking the field; Optional includes the field when present; Hidden only stops Pivot users seeing it while defining a report. |
| Required vs recommended CIM fields | Required fields must be populated for the dataset to work and are the basis of compliance; recommended fields improve coverage for Enterprise Security content but their absence does not exclude the event. |
| `scanCount` vs `eventCount` | `scanCount` is events read off disk; `eventCount` is the subset that matched the search terms. |
| `search` vs `where` | `search` compares a field to a literal, is case-insensitive on values, and accepts `*`; `where` compares field to field, is case-sensitive, has no `*`, and needs `like()` with `%` for prefix matching. |
| Search-time precedence vs configuration file precedence | Search-time precedence is the order in which nine kinds of object are applied to events; configuration file precedence is which copy of a duplicated setting wins the merge across `default`, app and user directories. Unrelated mechanisms. |
| Smart vs Fast vs Verbose | Smart is the default and discovers all fields only for non-transforming searches; Fast discovers only what the search needs; Verbose discovers everything and disqualifies a report from report acceleration. |
| Source type rename vs field alias | Renaming a source type in props.conf is search-time, moves the original to `_sourcetype`, and makes the original stanza's extractions stop applying; a field alias adds a name to a field and touches nothing else. |
| `span` vs `bins` | `span` sets the bucket size directly; `bins` sets a maximum bucket count and Splunk picks the smallest span producing no more than that. If both are given, `span` wins and `bins` is ignored. |
| `stats` vs `eventstats` vs `streamstats` | `stats` replaces events with a results table; `eventstats` adds the aggregate back onto every event; `streamstats` adds a running aggregate computed over events seen so far. |
| `summariesonly` vs `strict_fields` | `summariesonly` is on `tstats`, defaults to false, and restricts results to summarized data; `strict_fields` is on `datamodel`, defaults to true, and restricts the field list to defaults plus constraint fields. |
| Tag vs event type | A tag is a label on a field-value pair with no search behind it; an event type is a saved simple search whose matches receive an `eventtype` field value. Tags run at stage 9, event types at stage 8, so you can tag an event type but not the reverse. |
| Transaction vs `stats` | `transaction` builds a single grouped event with `_raw`, `duration` and `eventcount`, is centralized streaming and search-head only, and is bounded by `maxevents`; `stats` is transforming, distributes across indexers, and is the documented preference wherever it will do the job. |
| `tstats FROM datamodel=` vs `from datamodel:` | `tstats` uses `FROM datamodel=<model>.<root_dataset>` with an equals sign; `from` uses `from datamodel:<model>.<dataset>` with a colon. |
| `usenull`/`nullstr` vs `useother`/`otherstr` | `usenull` covers events missing the split-by field and labels them `NULL`; `useother` covers values that lost the `limit` or where-clause cut and labels them `OTHER`. Both true on `chart` and `timechart`, `useother` false on `top`. |
| `values()` vs `list()` | `values()` is distinct and lexicographically ordered with no cap; `list()` is every value in event order, capped at 100. |
| Workflow action vs alert action | A workflow action fires on a human click in a Splunk Web menu and changes nothing about the data; an alert action fires automatically when a saved search's trigger condition is met. |

## The last hour: fifteen facts

Read these in two minutes. Nothing here needs a sentence of context.

1. Search-time order, nine stages: field filters, `EXTRACT-`, `REPORT-`, `KV_MODE`, field alias (5), calculated field (6), lookup (7), event type (8), tag (9). Downstream sees upstream. Never the reverse.
2. `transaction`: `maxspan` `-1`, `maxpause` `-1`, `maxevents` `1000`. Negative deactivates. Fields added are `duration` (seconds), `eventcount`, `closed_txn`. `_time` is the earliest member event.
3. `closed_txn=1` requires `maxevents`, `maxspan`, `maxpause` or `startswith`. `endswith` does not count. Orphans are `_txn_orphan=1`; evicted are `closed_txn=0`. Both options default false.
4. A tag attaches to a field-value pair. Search syntax `tag::<field>=<name>` with two colons, or bare `tag=<name>`.
5. Event type priority: 1 best, 10 worst. Definition may not contain a pipe, a subsearch, `savedsearch`, or a tag reference.
6. A field alias adds a name and keeps the original. `rename` replaces the name for one search only. Alias scope is host, source, or source type. Nothing else.
7. All `EVAL-` in one props.conf stanza run in parallel, never chained. A calculated field overrides an extracted field of the same name even when it evaluates to null. Guard with `coalesce`.
8. Macros use backticks. A two-argument macro is stored as `mymacro(2)`. Arguments field holds bare names, Definition holds `$name$`. Macro expansion happens before stage 1 and is not in the nine.
9. Workflow action `type` is `link` or `search` only. `link.method` default `get`, `link.target` default `blank`, `display_location` default `both`, `fields` default `*` and the list is a conjunction. `search.preserve_timerange` is ignored if `search.earliest` or `search.latest` is set.
10. CIM: acceleration disabled by default on every model, add-on installs on search heads only, each model searches all indexes by default, and tags are what admit an event into a dataset.
11. Data models: only root datasets add auto-extracted fields. Required filters events out. Hidden only hides from Pivot. Dataset ID cannot be changed after save. Child constraints cannot contain macros.
12. `tstats summariesonly` defaults to `false`. `datamodel strict_fields` defaults to `true`. `FROM datamodel=M.D` for `tstats`, `from datamodel:M.D` for `from`.
13. `chart` `limit` default `top 10` and it caps columns; `timechart` `limit` default `top10` and takes one BY field. `chart bins=300`, `timechart bins=100`. `span` beats `bins`. `useother` is false on `top`, true on `chart` and `timechart`.
14. `NOT field=value` keeps events with no field; `field!=value` drops them. `search` evaluates OR before AND; `eval` and `where` evaluate AND before OR. `where` has no `*`; use `like()` with `%`.
15. `fillnull` default value `0`, no wildcards. `filldown` accepts wildcards and copies the last non-null value. Subsearch defaults: 10,000 results, 60 seconds, silent truncation. `rex max_match` default `1`, and `0` means unlimited.

## Needs an ID

None. Every trap in every `## Traps` block across the ten topic files, the foundations refresher and the four trap-defining reference files carries an ID, and all 292 are reproduced in the master table above. The ID blocks are contiguous with no gaps: T-00-01 to T-00-18, T-01-01 to T-01-20, T-02-01 to T-02-19, T-03-01 to T-03-20, T-04-01 to T-04-18, T-05-01 to T-05-15, T-06-01 to T-06-14, T-07-01 to T-07-14, T-08-01 to T-08-14, T-09-01 to T-09-19, T-10-01 to T-10-15, T-KO-01 to T-KO-20, T-TM-01 to T-TM-17, T-RX-01 to T-RX-12, T-CIM-01 to T-CIM-16.

Three traps carry an open `[verify]` marker inherited from their source file: T-04-17, on whether the Field Extractor lets you change method before saving; T-08-10, on whether workflow action names may contain spaces; and T-TM-05, on month-end clamping in relative time arithmetic. None is likely to decide a question on its own. The full inventory is below.

## Open verify markers

Everything in the guide still marked `[verify]`, in one place, so there is a single record of what has not been settled verbatim against `help.splunk.com`. A marker means the surrounding claim is the best reading available but was not confirmed word for word on a fetched page. None of these decides an exam question on its own, and most are on-screen UI label strings, which the 10.4 documentation describes in prose rather than quoting.

Refreshed 2026-07-28 after the topic upgrade: 20 marked lines. Line numbers drift if the files are edited.

| File | Line | The unsettled claim |
| --- | --- | --- |
| `cram/07-macros.md` | 27 | Definition: tokens as `$argname$`. Literal dollar sign: `$$` [verify]. |
| `reference/cim-data-models.md` | 314 | The complete cross-model membership rows for `src`, `src_ip`, `src_port`, `user`, `src_user`, `status`, `signature` and `vendor_product` on the "CIM fields per associated data model" page could not be read; the entries above are c |
| `reference/cim-data-models.md` | 329 | Prescribed values of `action` for Data Access, Data Loss Prevention, Email, Endpoint, Network Sessions and Performance were not read from the docs in this pass `[verify]`. |
| `reference/regex-for-splunk.md` | 9 | Splunk accepts **both** named capture group syntaxes. The same docs page shows `(?P<field_name>capture_pattern)` and `(?<field_name>capture_pattern)` as valid, and they are identical in effect: each creates a field named `field_na |
| `reference/spl-command-reference.md` | 245 | Runs the subpipeline against the results already in the pipeline and appends its output as extra rows. Unlike `append`, there is no subsearch and no second data retrieval, so the subsearch limits do not apply. `run_in_preview` def |
| `reference/spl-command-reference.md` | 349 | `collect` writes the current results into a summary index you name; `index` is required, and `testmode` defaults to `false`, so it writes for real. `addtime` defaults to `true` [verify]. `history` returns your own search history a |
| `reference/time-modifiers.md` | 312 | T-TM-05** `-1mon` from 31 March gives 31 February, or gives thirty days earlier. Wrong on both counts. `mon` is a calendar unit, so `-1mon` from 22 July is 22 June, not 22 June minus a day, and from 31 March there is no 31 Februar |
| `topics/00-foundations-refresher.md` | 72 | In the sidebar itself, string fields are marked with a lowercase `a` and numeric fields with a `#`. [verify] |
| `topics/00-foundations-refresher.md` | 285 | Trigger conditions for scheduled alerts are built-in result and field count options, plus a custom condition written as a secondary search. The UI names them Number of Results, Number of Hosts, Number of Sources, and Custom. [veri |
| `topics/00-foundations-refresher.md` | 311 | Report and data model acceleration are automatic once turned on. Summary indexing is a pipeline you own. Reports cannot benefit from report acceleration when run in Verbose mode. Reports created from Pivot cannot be accelerated. S |
| `topics/01-transforming-commands.md` | 202 | Series naming when a split-by field meets more than one aggregation: `chart` and `timechart` build one column per (aggregation, split value) pair, named through `format`, or through `sep` as the shorthand `format=$AGG$<sep>$VAL$`, |
| `topics/04-field-extractions.md` | 354 | T-04-17** A saved delimiter extraction can later be edited as a regular expression. Correct fact: the two methods produce different configuration objects. A saved delimiter extraction is a transform built on `DELIMS` and `FIELDS`, |
| `topics/05-aliases-and-calculated-fields.md` | 111 | Apply to \| `sourcetype`, `source`, `host` \| The docs state no default and treat the choice as required on both forms; `sourcetype` is pre-selected in the product [verify] \| Stanza type. A wildcard is accepted here to apply the  |
| `topics/06-tags-and-event-types.md` | 154 | Tags add nothing to the event body.** The one ordering statement in the docs is that Splunk "applies tags to field/value pairs in events in ASCII sort order", which is order of application, not precedence: no tag overrides another |
| `topics/06-tags-and-event-types.md` | 246 | An event type search string may reference another event type, but only when the inner one is processed first. The one ordering rule inside step 8 is that Splunk "processes event types first by priority score and then by lexicograp |
| `topics/06-tags-and-event-types.md` | 311 | Two consequences. A wildcard that preserves the original capitalisation, `tag=Priv*`, matches under either reading, so prefer it when it is offered. And you can **resolve this on your own instance in under a minute**: tag a pair w |
| `topics/07-macros.md` | 64 | The Splunk Web form is at Settings, then Advanced search, then Search macros, then the button to create a new macro (the documentation step reads "Click New"; in Splunk Enterprise 10.x the button is labelled New Search Macro [veri |
| `topics/07-macros.md` | 102 | Two escaping rules matter, and they pull in opposite directions. Inside a definition, `$argname$` is the substitution token, and a literal dollar sign is written by doubling it as `$$` [verify]. Inside a search, a macro reference  |
| `topics/08-workflow-actions.md` | 86 | The label strings for **Run in app**, **Open in view**, **Run search in** and **Use the same time range as the search that produced this event** are the working labels in Splunk Web; the 10.4 docs describe these four controls in p |
| `topics/08-workflow-actions.md` | 131 | `search.target` \| `blank`, `self` \| not stated in the spec; the spec says it works in the same way as `link.target`, whose default is `blank` [verify] \| Whether the constructed search opens in a new window or the current one. \ |

Several of these resolve the first time you open the relevant form or run a two-minute check on your own Splunk 10.x instance. The tag-name case sensitivity question behind T-06-12 is the one worth settling first, because three practice questions turn on it.
